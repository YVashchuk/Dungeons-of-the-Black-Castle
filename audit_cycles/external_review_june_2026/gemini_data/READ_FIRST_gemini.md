# Gemini — READ THIS FIRST (revised data pack, fixes the context-window problem)
**Why the previous attempt failed:** the raw `remake_data.js` is a single ~1 MB line and `mj_art.js` is ~6.9 MB (it embeds base64 images). Both exceed your context window, so your file reader only returned the first fragment. **That was correct behaviour on your part — do not parse those raw files.** This folder contains small, pre-extracted, fully-readable replacements. You do NOT need `remake_data.js`, `game_logic.js`, or `mj_art.js` at all.

## Files in this `gemini_data/` folder (all small, all readable)
- `02_graph_facts.json` (~1 KB) — PRECOMPUTED graph facts: total paragraphs, reachable-from-§1 count, unreachable list, dangling targets, the 76 combat-paragraph IDs, and any label/target mismatches. **Task 2 is essentially answered here — your job is to sanity-check it.**
- `03_combat_paragraphs_for_castle_review.jsonl` (~128 KB) — the 76 combat paragraphs, each with its own FB2 text + every inbound paragraph's text, and our current `in_CASTLE_SECTIONS` verdict. **This is all you need for Task 1.**
- `04_art_coverage.json` (~16 KB) — extracted `MJ_MAP` (paragraph→art), `MJ_META` scenes, the full `art_catalog.py` catalog, and the computed diffs between them (art used-but-not-cataloged, cataloged-but-unused, etc.). **This replaces both `mj_art.js` and the art catalog for Task 3 — the art catalog is now embedded here, so the "missing art catalog" blocker is resolved.**
- `01_paragraphs_part1.jsonl` … `part5.jsonl` (~150 KB each) — the FULL game, one JSON object per paragraph (n, text, choices, enemies, auto_items, grants_items/food, bet_payout, riddle), split into 5 readable shards covering paragraphs 1–300, 301–600, 601–900, 901–1200, 1201–1221. Use these only if a task sends you to a paragraph outside the 76 combat ones.
- (canon) `assets/fb2_remake.fb2` (~900 KB plain text) — readable; use for verbatim quotes. If even this is too big to load whole, the paragraph `text` fields in the shards above ARE the FB2 canon text (already stripped of markup), so you can quote from those.

## Your three tasks (same as `03_GEMINI_graph_castle_art.md`, now with exact file pointers)
- **Task 1 — CASTLE_SECTIONS (highest value):** open `03_combat_paragraphs_for_castle_review.jsonl`. For each of the 76 combat paragraphs, read its `text` + `inbound[].text` and classify INSIDE vs OUTSIDE the Black castle. Compare your verdict to the `in_CASTLE_SECTIONS` flag. Report (a) any flagged-INSIDE paragraph that is really forest/road/river, (b) any flagged-OUTSIDE paragraph that is really castle-interior. Our current set (26): 43,96,131,174,388,455,481,588,618,628,684,722,742,760,788,790,805,823,915,950,1050,1096,1099,1150,1163,1177. Pay attention to our hand-ruled ones: §617 (we say forest Застава → excluded), §191 (forest guide → excluded), §456 (forest spider-tree → excluded), §197 (forest she-bear den → OUTSIDE). Give the full 76-row table.
- **Task 2 — graph integrity:** read `02_graph_facts.json`. Confirm reachable=1205/1221, dangling=[] (none), and label_target_mismatches=[] (none). If you want to re-derive, the shards `01_*` give every paragraph's `choices[].target` — but the facts are already computed; just validate and flag anything inconsistent.
- **Task 3 — art coverage:** read `04_art_coverage.json`. Verify the coverage diffs (`in_MJ_MAP_but_not_in_catalog`, `in_catalog_but_not_used_in_MJ_MAP`), and review the `known_broken_or_flagged` list. Produce a clean paragraph→art→status table. Do not invent counts — everything is in this file.

## Output
Per task: a table or numbered list. Each finding: **claim → FB2 quote (paragraph N — quote from the `text` field) → suggested action**. Separate "verified, no issue" from "problem found". For Task 1, give the full 76-paragraph classification so we can diff it against ours. Quote only what is in these files; if something truly isn't here, say so (but the art catalog and all paragraph text now ARE here).
