# Верификационный аудит «Подземелий Чёрного замка»

Дата цикла: 2026-07-20  
Аудитируемое состояние: `main` @ `2179619e11c277ad74f969e8e76f403d89e6b752`, реестр `v2.139`  
Корпус: приложенный архив `Dungeons-of-the-Black-Castle-main(2).zip`; через GitHub подтверждены приватный репозиторий, ветка, точный SHA и совпадение контрольного blob `assets/GAME_RULES.md`  
Режим: строго read-only; внешний веб-канон не использовался

Итог цикла: **12 проверяемых гипотез — 7 P0, 3 P1, 2 P2**. Полная штатная батарея остаётся зелёной, но детерминированные проверки взаимодействий нашли ошибки, которых текущие shape-harness-ы не исполняют.

## 1. VERIFIED-OK

### V. Верификация исправлений

Проверены **16/16** резолюций group_78. Полностью, без оговорок, сошлись **8/16**: G-01, G-02, G-05, G-08, G-10, G-13, E-01 и F-01. В остальных восьми базовые данные в основном присутствуют, но обнаружены расхождения реализации или взаимодействий, перечисленные в разделе 2.

- **Дедлайны — 4/4.** Конфиги §§43/261/737/1099 дословно соответствуют FB2 и резолюции. Выполнены восемь граничных трасс: победа в раунде `N` ведёт соответственно в §§1082/520/391/894; живой враг на старте `N+1` ведёт в §§1016/8/182/смерть. §1099 действительно завершает просрочку death-overlay, а §737 продолжает канонический бой через §182.
- **`joins` — 9/9 маркеров в 7 боях.** Изолированно верны `after_death` §§177/588/617, `round:11` §388, OR-триггеры обоих орков §628 (`round:4` или смерть лидера), `round:7` и `after_deaths:2` §742, `after_deaths:5` §790. Конвенция «через N раундов» = раунд `N+1` выдержана. Ошибки возникают не в конфиге триггеров, а при преждевременной проверке победы и нераундовых убийствах — X-01.
- **Кубиковые механики — 3/3 базовых resolver-а.** §781 скрывает свободный выбор и вычисляет `2d6 >= 10`; §725 при каждом броске снимает 1 ВЫН, сохраняет потерю, оставляет отходы и убивает на нуле; §932 бросает d6, учитывает семь слотов и расходует одну личинку на текущую цель. Незакоммиченные окна roll/reload вынесены в V-04.
- **Боевые модификаторы.** Все **10/10** `player_attack_mod` находятся в нужных §§3/21/46/65/368/436/448/528/656/722. Все **4/4** `combat_condition` штатно появляются после обычного раунда; обычная полная победа выставляет `S.combatCondMet`. Все **14** мостов `combat_mod` присутствуют, включая три `ENEMY_WEAK_PICK`; §536 подтвердил, что дебафф остаётся на выбранной в раунде 1 цели после последующей смены цели. Цепь 449→1183 не получает эффекта, как и требует канон.
- **Списание заклятий — 3/3.** Downstream-переходы 526→436, 1183→532 и 1021→715 не имеют `spell`-тегов; канонический каст списывается только в источнике. Net-модификатор §436 с активным флагом Силы, мечом и щитом равен `+1 +1 +1 = +3`.
- **G-08 — 16/16 эффектов.** На базе `STAMINA=20, SKILL=10, LUCK=5` получены точные дельты §§123 −5, 199 −9, 219 −2, 230 −8, 273 −4, 278 −2, 522 LUCK+1, 525 −1, 561 −1, 606 +6, 864 LUCK+1, 1036 SKILL−1/STAMINA−2, 254 +8, 959 +3, 981 +7, 1061 +7. Отдельно §§199/230 при STAMINA 5 дали 0 и death-overlay; cap/floor реализованы общим обработчиком.
- **Экипировка и зелье.** При наличии предметов меч Зелёного Рыцаря и щит дают +1/+1 только в формуле боя, поверх изначального cap; «Смерть Орков» не даёт бонуса СУ, убивает первого активного орка до раунда 1, активирует staged-врагов и поддерживает полный wipe. Зелье даёт +5 при положительном `dragonKillsLeft`; детерминированно: после убийства Копией `3`, медведем `3`, личинкой `2`. Проблемы обязательной выдачи и замены меча — V-02/G2-01.
- **G-12.** Конфиги всех **8/8** параграфов и пять self-loop `pickup_batch` соответствуют резолюции; §582 сохраняет round-robin, §§585/801 дают две единицы, §724 — верёвку и золотую стрелу, §1140 — вино(3), еду(2), свисток. `pickup_batch` соблюдает overflow, остаётся одноразовым после save/load и на ревизите показывает «✓ Собрано». Исполнение food-object §389 и имена четырёх новых slug-ов — V-01/R-02.
- **G-13 — 6/6 решений.** §486 даёт 1 зол. и снимает УДАЧУ; §510 выдаёт адьюдицированные 85; §812→675 скрыт при `<2` зол., затем снимает 2 и лечит 5; §770 даёт 4; §873→922 требует и снимает 1, §922 лечит 2. Отложенный free-pick §63 не перелитигировался.
- **E-01/E-02/F-01.** Команды `tests/README.md` работают из корня, dist-проверки документированы пофайловым циклом. README содержит актуальные 179 `auto_items`, корректный damage-census и выбор цели. EN §§417/178, UK §§131/1123/772 совпали с резолюцией; RU §§1113/1131 сохранены FB2-faithful, числовой паритет не изменён. Единственная документная дельта — метка версии GAME_RULES в R-03.

### X. Взаимодействия механик

Исполнены **13/13 обязательных семейств пар** из брифа и шесть дополнительных пересечений — всего **60 детерминированных сценариев** на собранном `dist` в DOM-эмуляции, без JS-исключений. **8/13** обязательных семейств дали ожидаемый результат end-to-end; пять выявили находки.

- §43 + «Смерть Орков»: первый орк мёртв в раунде 0, второй доживает до старта раунда 11 — маршрут §1016. Две личинки завершают бой в раунде 0 — §1082. Копия в §1099 завершает бой в раунде 0 — §894.
- `joins` после убийств личинкой и «Смертью Орков» активируются; OR §628 и обе ступени §742 корректны. Копия/союзник и победа до round-only подключения выявили X-01.
- Выбранный в обычном multi-бою `ENEMY_WEAK_PICK` не переносится при смене цели; ограничение staged-карточек выявлено в V-03.
- Счётчик «Силы Дракона» не тикает от Копии и союзников, но тикает от личинки и собственного удара героя.
- §436 правильно складывает скриптовый `+1` с экипировкой `+2`; отдельное окно render/reload — X-04.
- `pickup_batch` переживает save/load/revisit; §725 сохраняет STAMINA 0 до показа смерти; пересечение `combat_condition × round_deadline` пусто по данным и конфликтов не создаёт.
- Hash-входы проверены на представителях всех новых классов (§§43, 71, 177, 261, 388, 389, 436, 532, 536, 585, 617, 628, 725, 737, 742, 781, 790, 801, 932, 994, 1099, 1130, 1140, 1175, 1213). State-setup запускается; выявленные отклонения совпадают с V-01—V-04 и X-01—X-04.

### G2. Обогащённый обратный поиск по FB2

Проверены все **1221** игровые секции (1220 секций с `id=n_N` плюс не-ID §1) и вводный раздел экипировки. Автоматический поиск дал 109 параграфов с приказами изменения статов, 9 с явным словом «раунд», 51 с ограничительными конструкциями и 75 с правилами предметов; затем каждый hit проверен вручную по FB2, леджеру и GD/движку.

- **109/109** stat-кандидатов объясняются базовыми правилами, существующими `auto_items`, боевыми модификаторами, удачей, ставками либо закрытой адьюдикацией. Новых пропущенных числовых транзакций после G-08/G-13 не найдено.
- **9/9** временных боевых формулировок каталогизированы: четыре `round_deadline` (§§43/261/737/1099), два прецедентных скрипта (§§131/1175), три declarative-joins (§§388/628/742).
- В 51 restriction- и 75 item-rule-кандидатах новых незадокументированных веток не найдено. Новое точное доказательство обнаружено во вводном правиле о невесомом оружии; оно оформлено как G2-01. Специфичное право §389 «можете съесть сразу» входит в V-01.
- Закрытые решения §§526, 482, 932, 774, 1113 и 1131 не переоткрывались без нового доказательства.

### R. Регрессия блоков A–F

- **4884/4884** paragraph-object загружены (`1221 × 4`); отсутствующих pid и caption-index ошибок нет. Выполнено **10 317/10 317** сравнений числовых мультимножеств — ошибок 0.
- Проверены **2218** обычных choice-edge и **14** riddle-target (`2232/2232` существующих целей), BFS остаётся `1205/1221` с прежними 16 островами.
- На каждой локали: `ui 314`, `enemies 66`, `map 43`, `spells 8`, `allies 2`; наборы UI-ключей совпадают в **942/942** межъязыковых сравнениях. EN/FR не содержат кириллицы; ASCII-апострофов 0 во всех локалях. Типографские апострофы: EN 261, FR 4735, UK 291.
- FR: 1115 NNBSP перед `? ! ;`, 763 NBSP перед `:`, нарушений соответствующего стандарта 0. Новые UI-ключи и captions входят в эти проверки.
- Объекты `GD` и RU/EN/FR/UK в `dist` эквивалентны `src` — **5/5** точных сравнений. `_dist_art_check` подтвердил 75 файлов `dist/art`.
- `SMOKE_TEST_PATHS.md`: **127/127** сценариев, **2622/2622** click-edge и все §-ссылки имеют существующие pid. Полный ручной клик-проход не выполнялся.
- Повторно просмотрена фиксированная литературная выборка прошлого цикла: **20 текстов на язык / 80 всего**. Новых смысловых или числовых расхождений не найдено; F-01 проверен отдельно на всех семи затронутых/адьюдицированных параграфах.
- Реестр синтаксически валиден: 141 запись `version_history`, 79 групп, group_78 = 16 DONE; group_79 = UI-01 DONE + 11 OPEN по решению пользователя. UI-02…UI-12 не переиздавались как находки.
- Полная батарея: **16 harnesses / 443 assertions**, baseline 1205, exit 0. Шесть dist-check-ов: **59 assertions**, каждый exit 0. Источники корпуса после аудита совпадают с чистой распаковкой; созданный обязательной установкой игнорируемый `tests/node_modules/` в сравнение не включён.

### U. UI-01

Проверено **1/1** требуемое исправление, статически сошлось.

- `.choices-area`: `overflow-y:auto`, fallback `52vh` и `52dvh`, `overscroll-behavior:contain`; portrait phone `34vh/34dvh`, landscape phone `44vh/44dvh`.
- `.main` сохраняет `overflow:hidden`, `.story-area` — собственный `overflow-y:auto`; изменение локализовано в области вариантов.
- Правила присутствуют в `dist`; `_dist_ui_check` содержит и проходит шестнадцатый assert `max-height:52dvh;overscroll-behavior:contain`.
- Реальный viewport/scroll жестами не проверялся; это ограничение указано в NOT-CHECKED. UI-02…UI-12 намеренно вне скоупа.

## 2. FINDINGS

Каждая строка ниже — **гипотеза для адьюдикации**, а не автоматически признанная ошибка. При несогласии с закрытой резолюцией приведена её точная цитата.

| id | блок | severity | §§/ключи | evidence | suggested fix |
|---|---|---|---|---|---|
| V-01 | V/X | P0 | §389; `auto_items.items[{food,stamina}]`; `canonItem`, `invDisplay`, `takeItem` | G-12 утверждает дословно: **“sec.389 introduces food-object entries ({food, stamina}) so the melon lands edible at +4.”** В реальном hash-входе модалка показывает `НАЙДЕНО:[object Object]`; после «Взять» в `S.inventory` лежит `{food:"melon",stamina:4}`, HUD показывает `[object Object]`, кнопки еды нет. Движок распознаёт только `{kind:"food",id,...}`. Дополнительно FB2 §389: «Если хотите, можете съесть сразу»; при полном мешке модалка имеет только disabled «Взять», поэтому немедленное съедание невозможно даже после простой конвертации формы. | Нормализовать `{food,stamina}` в `{kind:"food",id:food,stamina}` до dedupe/modal; для food-offer добавить действие «Съесть сейчас», не требующее слота и соблюдающее cap/запрет боя. Тесты: пустой/полный мешок, take/eat, save/load, четыре локали. |
| V-02 | V | P0 | §§71, 1213; `death_of_orcs`, `knight_shield`; обязательная выдача | G-09 говорит дословно: **“sec.71 'Death of Orcs' auto-granted (imperative swap)”** и **“sec.1213 shield auto-granted as knight_shield.”** Оба предмета идут через закрываемую добровольную `showInventoryModal`: закрытие без «Взять» и Continue оставляют `inventory=[]` и ведут в §755; при полном мешке кнопка также блокируется. При уже имеющемся `whole_sword` выдача §71 не удаляет старый меч: получаются оба меча и обе выгоды, хотя FB2 §71 требует «Оставьте свой меч в кладовой вместо того, который берете с собой». | Ввести обязательную атомарную acquisition/equipment-операцию: §71 удаляет текущий меч, выдаёт `death_of_orcs` и не позволяет продолжить без завершения swap; §1213 гарантированно экипирует щит. При необходимости открыть выбор выбрасываемого обычного предмета, но не разрешать silent skip. |
| V-03 | V/X | P0 | §§116→1175, 655→628; `ENEMY_WEAK_PICK`, `active:false`, `targetIdx` | G-06 обещает дословно: **“the player can click any enemy card before the first strike.”** В §§1175 и 628 до раунда 1 активен только лидер; клики по двум ожидающим карточкам игнорируются. Принудительный `targetIdx=1` валидатор сбрасывает в 0, и `weakDebuff=-2` всегда получает лидер. Это противоречит FB2 «любого из Орков/любого из них». Контрольный §470→536 работает: выбранная активная цель получает −2 и дебафф не переносится. | Отделить выбор цели предбоевой Слабости от текущей боевой targetability: до первого удара сделать все живые карточки допустимыми именно для `pendingWeakPick`, зафиксировать выбранный enemy-index, затем вернуть обычное правило active-target. Тесты обоих staged-мостов и §536. |
| V-04 | V/X | P0 | §§781, 932; `dice_check`, `dice_loot`, `S.diceLootDone`, save/load/hash | G-11 для §932 обещает дословно: **“sets a persistent S.diceLootDone flag so revisits show only Continue.”** После броска 4 можно нажать Continue без подбора: флаг остаётся пустым, hash/revisit снова показывает «🎲 Бросить кубик». F5 после броска также перебрасывает. В §781 после показанного провала save не содержит результата; reload снова показывает «Бросить кубик», позволяя повторять обязательную проверку до успеха. §725, напротив, сохраняет каждую потерю ВЫН корректно. | Коммитить результат броска до показа исхода: отдельные persisted records для §781 и §932. §781 на reload восстанавливает один resolved target; §932 восстанавливает тот же `n` и незавершённый pickup, а Continue явно фиксирует отказ/`done`. Добавить F5/revisit/hash тесты. |
| X-01 | X | P0 | §§177, 388, 588, 617, 628, 742, 790; `joins`; Копия/союзники; `getAliveCombatEnemies` | G-11 формулирует инвариант дословно: **“victory check spans ALL enemies incl. staged ones so pending phases keep the fight alive.”** В §177 Копия убивает льва: львица остаётся `hp=8, active=false`, но `combatDone[177]=true` и появляется «Продолжить»; `useAllyInCombat` имеет тот же пропуск `activateStagedJoins`. В §388 при двух мёртвых активных рыцарях и ожидающем третьем `combatRound()` также завершает победой до round 11. Причина: центральные проверки используют только `getAliveCombatEnemies`, исключающий waiting. | Ввести единый `combatResolved(cs)` по всем не-fled врагам и использовать его во всех путях убийства/победы. После Копии и союзника вызывать `activateStagedJoins`; при round-only waiting без активных врагов оставлять бой открытым с явным «Ждать/следующий раунд», пока не наступит триггер. Покрыть каждый тип убийства и ожидания. |
| X-02 | X | P0 | §617→165; `combat_condition:enemy_active_1`; `#combat-condition-btn`; `clearCombatExtraButtons` | G-03 обещает дословно: **“flee-conditions are excluded from the post-victory Met flag (no phantom escape after winning)”**. С `death_of_orcs` гоблин активируется до раунда 1, но кнопка бегства появляется лишь после ещё одного обычного раунда. После полной победы она остаётся рядом с Continue, а после запуска нового боя §43 остаётся там же с прежним handler-ом §617→165. Нажатие может списать 2 ВЫН, пометить завершённым уже новый бой и увести в неверный параграф. `clearCombatExtraButtons()` удаляет другой wrapper, но не `#combat-condition-btn`. | Управлять condition-кнопками одним контейнером: очищать на start/end/routed end/navigation, пересчитывать сразу после любого изменения hp/active (меч, личинка, Копия, союзник, join) и привязывать handler к immutable section/combat instance. Добавить тест «нет после victory и в следующем бою». |
| X-03 | X | P0 | §§532, 994; личинка/Копия/союзник × `wound_2`; `S.combatCondMet` | G-04 описывает исходный дефект дословно: **“fully killing the sec.532 dragon (or now the sec.46 pair / sec.994 spirit) stranded the player with zero buttons.”** Обычное убийство исправлено через число ран, но мгновенная личинка в §§532/994 даёт `wounds=0`, вызывает `endCombat(true)`, не ставит флаг; после Continue `visibleChoices=[]`. Копия/союзник проходят тот же путь без увеличения `wounds`. Новая механика снова открыла ровно исправлявшийся тупик. | При полной победе считать все non-flee `combat_condition`-выходы доступными либо добавить семантику `enemy_dead ⇒ wound_2 satisfied for navigation`. Применять до render post-combat независимо от способа убийства. Тесты trade/larva/Copy/ally для §§532/994. |
| X-04 | X | P1 | §436→526→436; `S.sec436_force`; reload-window; экипировка | G-07 ожидает дословно: **“the return to 436 re-fights at +1 with no re-cast.”** При рендере вернувшегося §436 ветка немедленно делает `S.sec436_force=false; saveGame()` ещё до клика «Драться». Net при клике верен (`playerMod=1`, equipment=2), но F5 между рендером и кликом теряет уже оплаченную Силу и возвращает pre-luck flow. | Не очищать persisted-флаг при построении кнопки; переносить consume в атомарный `onclick` старта боя либо хранить committed combat setup, восстанавливаемый после reload. Тест до/после рендера и с +0/+1/+2 экипировки. |
| G2-01 | G2 | P1 | вводный раздел FB2; §§35/71/542; `whole_sword`, `death_of_orcs`; `getBagUsed` | Новое прямое доказательство FB2: **«Обратите внимание, что деньги и оружие в заплечный мешок не кладутся»**. Оба новых/переиспользованных меча лежат в `S.inventory`; `getBagUsed()` считает каждый за один слот. Трасса с двумя мечами дала `bagUsed=2`. Это не описано как упрощение в G-09/GAME_RULES и может блокировать канонические pickups. | В реестре ввести `slotCost:0`/`kind:"weapon"` и получать размер из registry вместо неполного `ITEM_SIZES`; мигрировать `whole_sword` и `death_of_orcs`. Отдельно сохранить атомарный swap V-02. Тесты 7/7 слотов и приобретения оружия при полном мешке. |
| R-01 | R/X | P1 | магазины (`purchase` 36); `pickup_batch` 5; `renderChoices→makeChoiceBtn`; `shopBought`, `batchPicked` | В финальных ветках `renderChoices` вызывается `makeChoiceBtn(ch)` без исходного индекса. Покупка одноразового `wood_piece` в §340 сохраняет `shopBought:{"340":[null]}`: после reload кнопка снова активна и повторно снимает золото, хотя предмет уже есть. `pickup_batch` сохраняет ключ `585:undefined`; сегодня один batch на параграф, поэтому one-shot работает, но два batch-а столкнутся. | Во всех render-ветках сохранять исходный индекс `sec.choices` и передавать его в helper-ы. Добавить round-trip JSON tests для всех item-purchase и batch; предусмотреть миграцию legacy `[null]`/`:undefined` saves. |
| R-02 | V/R | P2 | `src/registries/items.json`; runtime `SLUG_TO_RU`/`RU_TO_SLUG`; `melon`, `coconut`, `bun`, `provisions` | G-12 сообщает дословно: **“Four food slugs registered (melon/coconut/bun/provisions)”**. В registry действительно 105 записей/20 food, но обе runtime-карты имеют лишь 101 и пропускают все четыре новых slug-а. После batch HUD показывает `coconut (еда: +3)`, `bun (еда: +2)`, `provisions (еда: +2)` вместо RU-имён; `melon` после исправления V-01 попадёт в тот же путь. | Генерировать runtime display map из `items.json` либо добавить четыре записи и guard `registry keys == runtime keys`; проверить отображение food во всех локалях. |
| R-03 | V/R | P2 | `assets/GAME_RULES.md` status; registry `v2.139`; E-02 | E-02 называет файл дословно **“a LIVING reference at assets/GAME_RULES.md, fully regenerated from the live census”**, а бриф требует синхронизацию с v2.139. Заголовок живого файла всё ещё говорит **v2.138**, хотя `version_history` заканчивается `v2.138 -> v2.139`. Числа механик актуальны; дефект метаданных. | Обновить status до v2.139 и генерировать/проверять номер из последнего `version_history` перехода. |

## 3. COUNTS

### Корпус и инварианты

| Метрика | Результат |
|---|---:|
| SHA-256 приложенного архива | `ab067dc1192f6abd20b7e5c6612f59a83efb6dad2c17d7cf67dcbdbbfab551d5` |
| Параграфы RU/EN/FR/UK | 1221 × 4 = 4884 |
| Choice-edge / riddle-target | 2218 / 14 |
| Все проверенные цели | 2232, dangling 0 |
| BFS достижимо / всего | 1205 / 1221 |
| Числовые сравнения локалей | 10 317, ошибок 0 |
| UI key-set comparisons | 942, ошибок 0 |
| UI / enemies / map / spells / allies на локаль | 314 / 66 / 43 / 8 / 2 |
| Боевые pid / враги / multi-enemy | 76 / 120 / 24 |
| Source↔dist object equality | 5 / 5 |
| SMOKE scenarios / click-edge | 127 / 2622 |
| Литературная выборка | 20 на язык / 80 всего |

### Каталог механик

| Подсистема | Проверено |
|---|---:|
| `round_deadline` | 4/4 конфига; 8/8 boundary-трасс |
| `joins` | 9/9 isolated triggers в 7 боях; interaction-дефект X-01 |
| `player_attack_mod` | 10/10 |
| `combat_condition` | 4/4 конфига; lifecycle-дефекты X-02/X-03 |
| `combat_mod` / `ENEMY_WEAK_PICK` | 14 / 3; staged-дефект V-03 |
| Dice resolvers | 3/3 базовых; persistence-дефект V-04 |
| Снятые downstream spell-теги | 3/3 |
| G-08 stat effects | 16/16 |
| G-12 paragraphs / `pickup_batch` | 8 / 5; §389-дефект V-01 |
| G-13 решения | 6/6 |
| `auto_items` / `acquires` | 179 / 15 |
| Spell hooks / `spell_any` | 100 / 3 |
| Inventory conditions / consume | 131 / 32 |
| Gold conditions / costs / purchase | 34 / 59 / 36 |
| Registry items / food | 105 / 20 |

### Исполняемые проверки

| Набор | Объём | Результат |
|---|---:|---:|
| `tests/run_all.js` | 16 harnesses / 443 assertions | exit 0 |
| BFS внутри батареи | 1205 | совпал |
| `_dist_*_check.js` | 6 файлов / 59 assertions | все exit 0 |
| Детерминированные interaction-сценарии | 60 | выполнены, JS errors 0 |
| Reverse-search FB2 | 109 stat / 9 round / 51 restriction / 75 item-rule hits | все триажированы |

### Находки

| Severity | Количество |
|---|---:|
| P0 | 7 |
| P1 | 3 |
| P2 | 2 |
| **Всего** | **12** |

## 4. NOT-CHECKED

- Визуальное качество изображений и `assets/illustrations/` не проверялись по прямому исключению брифа. Наличие/целостность `dist/art` проверены только `_dist_art_check`.
- Не выполнялся реальный браузерный UI-прогон, шесть viewport-ов, touch/scroll, звук, PWA-install и service worker. Динамические механики исполнялись в DOM-эмуляции; UI-01 подтверждён статически и dist-assert-ом.
- Не выполнен ручной end-to-end клик всех 127 SMOKE-маршрутов. Их 2622 шага проверены структурно; точечные новые взаимодействия покрыты 60 детерминированными сценариями.
- Не проводилась полная литературная редактура всех 4884 текстов: повторена фиксированная выборка 80 текстов и полные автоматические i18n-инварианты.
- Windows-батарея из брифа не запускалась повторно; независимо подтверждён текущий Linux-прогон.
- `build.sh` не запускался, чтобы не изменять корпус. Вместо rebuild проверены точное равенство пяти встроенных data-object и все шесть предоставленных dist-check-ов.
- Полное побайтовое сравнение каждого GitHub blob с архивом не выполнялось; подтверждены точный commit SHA и контрольный blob GAME_RULES. Архив оставался источником исполняемого корпуса.
- UI-02…UI-12 group_79 сознательно не проверялись и не перечислялись как находки.
- Внешние сайты, сторонние издания и веб-канон намеренно не использовались.
