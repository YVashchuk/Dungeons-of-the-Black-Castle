# BRIEF — верификационный аудит-цикл «Подземелья Чёрного замка» (2026-07-20)

**Аудитируемое состояние:** ветка `main`, коммит **`2179619`**, реестр **v2.139** (79 групп).
**Контекст цикла:** это цикл ПОСЛЕ реализации всех 16 находок вашего отчёта 2026-07-14 (group_78 закрыта 16/16) и P0 UI-аудита (UI-01). Задача теперь тройная: (V) проверить сами исправления против канона и кода, (X) целенаправленно поохотиться на взаимодействия новых механик, (G2) повторить обратный поиск по FB2 с обогащённым каталогом. Полный набор верификации на `2179619` зелёный (16 харнессов / baseline 1205 + шесть dist-чеков, Windows и Linux).

**Режим:** строго read-only; результат — один `REPORT.md` по формату раздела 7.

---

## 1. Корпус

GitHub-архив ветки `main` @ `2179619` (пользователь выгружает сам). Примечания:
- В отличие от прошлого цикла, архив содержит `assets/art/` и `dist/art/` — **`_dist_art_check.js` в этот раз запускается** (все шесть dist-чеков обязаны дать exit 0).
- `assets/illustrations/` (~407 MB Midjourney-оригиналов) к проверкам не относится — игнорируйте её целиком; визуальное качество изображений по-прежнему вне скоупа.

## 2. Канон и порядок истины (без изменений)

Русская книга — источник правды; иерархия: `assets/text_corrections.json` (леджер адьюдицированных решений — приоритет) → `assets/fb2_remake.fb2` → `assets/book_1991_extracted.txt`. Переводы сверяются с RU-локалью.

## 3. Обязательное чтение

1. **`assets/GAME_RULES.md`** — ЖИВОЙ справочник правил, синхронизирован с v2.139: полный каталог всех механик, включая добавленные этим циклом (дедлайны, joins, dice-резолверы, выбор цели, мосты, экипировка/зелье, pickup_batch). Это ваш эталон блоков V/X/G2. Замороженный снапшот прошлого цикла — в `audit_cycles/full_audit_2026_07_14/`.
2. **`assets/text_corrections.json`, группы 78 и 79** — резолюции пунктов G-01…G-13/E-01/E-02/F-01 и UI-01 являются **спецификациями исправлений**: каждая описывает принятое каноническое прочтение, механизм и осознанные упрощения. Проверяйте реализацию ПРОТИВ резолюции; несогласие с самой резолюцией оформляйте находкой с новым доказательством и точной ссылкой на пункт.
3. Ваш прошлый цикл: `audit_cycles/full_audit_2026_07_14/REPORT.md` + `UI_AUDIT.md` (заморожены).
4. `SMOKE_TEST_PATHS.md`, `tests/README.md` (переписан: запуск от корня, dist-чеки — пофайловым циклом).

**Закрытые адьюдикации не перелитигировать без новых доказательств.** В частности: §526 — регрессия ПОДТВЕРЖДЕНА и устранена повторно (запиннена); §482 перо — ПОДТВЕРЖДЕНО и выдано; §932/group_18 — ошибка атрибуции (group_18 — кусочек дерева §774); RU §1113/§1131 — явное решение «FB2-faithful, без изменений».

## 4. Как запускать верификацию

От корня репозитория: `npm --prefix tests install`, затем `node tests/run_all.js` (ALL GREEN, 1205), затем шесть `_dist_*_check.js` пофайловым циклом (каждый exit 0). Glob одному `node` не передавать (исполнится только первый файл). Fallback `python`→`python3` автоматический.

## 5. Скоуп аудита

**V. Верификация исправлений (приоритет 1).** Для КАЖДОГО пункта каталога `GAME_RULES.md` §§3.6–3.8, 6–7 и каждой резолюции group_78 подтвердите тройное соответствие «канон FB2 ↔ резолюция ↔ код/данные»: 4 `round_deadline` (семантика прерывания на N+1, win/lose цели, "death" §1099); 9 `joins`-маркеров в 7 боях (виды триггеров, OR-семантика §628, конвенция «через N раундов» = раунд N+1); 3 кубиковые механики (§781 порог, §725 цикл −1 ВЫН/дубли/смерть на нуле/сохранённые отходы, §932 d6/слоты/боевой расходник); 10 `player_attack_mod`; 4 `combat_condition` (+пост-победный показ по флагу — фикс тупика §532); 3 моста `ENEMY_WEAK_PICK` (targeted −2 на цель раунда 1, лог, отсутствие эффекта у 449→1183 по канону); снятые double-spend теги (526/1183/1021 — источники тратят ровно один раз); 16 stat-эффектов G-08; экипировка (+1/+1, «сверх изначального» как боевой термин, «Смерть Орков» — первый активный орк до раунда 1, staged-эскалация, полный вайп = победа) и зелье (+5 до трёх СОБСТВЕННЫХ убийств героя: торговый удар/меч/личинка; Копия и медведи исключены); 8 параграфов G-12 (модалка-предложение, food-объекты, pickup_batch: слоты/однократность/self-loop); 6 фиксов G-13 (включая гейт 812→675 и рыбу 873→922); E-01/E-02/F-01 (включая близнеца §772).

**X. Взаимодействия механик (приоритет 2, новый блок).** Систематический парный проход по механикам из `GAME_RULES.md` §§2–8 с поиском конфликтов/дыр на пересечениях. Обязательные пары (не исчерпывающий список — ищите свои): deadline × «Смерть Орков» (§43 — орки + дедлайн: мгновенный килл на старте и счёт раундов); deadline × победа Копией/личинкой (маршрутизация win при не-торговых убийствах); joins × личинка/меч (активация после нераундовых смертей); joins × flee и × победа до активации ожидающих; `ENEMY_WEAK_PICK` × смена цели G-05 (дебафф лёг на цель раунда 1 — смена цели после не переносит его); зелье × Копия/союзники (тик НЕ должен срабатывать); `sec436` скрипт × экипировка (net-модификаторы с мечом/щитом — сверить с каноном §436); pickup_batch × переполнение × save/load × ревизит; dice_bash × смерть на нуле × сейв; условные выходы × deadline-секции; авто-модалка предложения × food-объекты × ёмкость; hash-вход (#N) × каждая новая механика (state-setup корректен?).

**G2. Свежий обратный поиск (приоритет 3).** Повторите поиск по RU/FB2 приказов «вычтите/прибавьте/только если/не успеете/за N раундов/предмет действует…», НЕ покрытых обновлённым каталогом — остаток после прошлого прохода. Также: проверьте, не появилось ли в новых механиках отступлений от буквы FB2, не задокументированных в резолюциях.

**R. Регрессия блоков A–F.** Повторите структурные/i18n/док-проверки прошлого цикла. Базовые числа ЛЕГИТИМНО изменились — сверяйте дельты с changelog v2.126→v2.139, а не с прошлым отчётом: ui 314×4, auto_items-секций 179, spell-хуков 100 (+3 spell_any), acquires 15, рёбра выросли на self-loop'ы pickup_batch и второй выбор §35, кэпшны добавлены во всех четырёх локалях синхронно (паритет обязателен), items.json 105 (еды 20). Апострофы/FR-пунктуация — прежние стандарты, включая НОВЫЕ ui-ключи и кэпшны.

**U. UI.** Проверьте только реализацию UI-01 (dvh-кап + overflow `.choices-area`, §132/§340, регресс-assert в `_dist_ui_check`). Пункты UI-02…UI-12 группы 79 ОТКРЫТЫ по дизайн-решению пользователя — **не** перечисляйте их находками повторно.

## 6. Севериті

P0 — исправление не соответствует канону/резолюции либо взаимодействие ломает навигацию/данные; P1 — механика с заметным эффектом, недокументированное отступление; P2 — косметика/доки/тесты.

## 7. Формат отчёта

Один `REPORT.md`: **1. VERIFIED-OK** (по блокам V/X/G2/R/U, с объёмами: сколько пунктов проверено и сошлось) → **2. FINDINGS** (таблица id | блок | severity | §§/ключи | evidence — с точной цитатой резолюции при несогласии | suggested fix; каждая строка — гипотеза) → **3. COUNTS** → **4. NOT-CHECKED**. Внешний веб-канон не использовать.
