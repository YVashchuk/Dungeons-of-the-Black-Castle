# REPORT — Track B (mechanics regression recheck) — recheck cycle 2026-09
**Project:** «Подземелья Чёрного замка» (JS remake) · **Auditor:** Claude (external auditor session, Windows MCP, read-only) · **Date:** 2026-09-04
**Mode:** strictly read-only, diagnostic. Every runtime claim below is **by code reading** of the repository files at HEAD; nothing was "tested" in a browser. Findings are hypotheses for the maintainer's re-verification.

---

## 0. PRE-FLIGHT

| Check | Result |
|---|---|
| `git rev-parse --short HEAD` | **`79124a5`** (expected `79124a5` — match) · branch `main` |
| `git status --short` | **empty** (clean tree; still empty after the battery run) |
| Brief delta | `BRIEF.md` names `459d8a1` / v2.155. `459d8a1` is an ancestor of HEAD (`git merge-base --is-ancestor` exit 0). Three commits since: `a051c30` docs, `0dfb2ac` docs, `79124a5` "fix(a11y): group_81 batch 1". `git diff --stat 459d8a1..HEAD`: **`src/game_structure.js` and `src/registries/items.json` unchanged**; `src/game_logic.js` +35 lines, a11y only (`_bcDialogStack`, `BC_COMBAT_STATUS` mirror, inventory `<span>`→`<button>`); locales +3 `ui` keys each (`ui_aria_map/log/menu`); registry v2.155→v2.157; `GAME_RULES.md` label follows. No mechanic changed after the brief was written. |
| Files read | `src/game_structure.js` 161 376 B (1 line) · `src/game_logic.js` 172 938 B (3 305 lines) · `src/registries/items.json` 7 965 B · `assets/GAME_RULES.md` 15 656 B · `assets/book_text.md` 925 841 B (13 772 lines) · `audit_cycles/recheck_2026_09/REGISTRY_EXCERPT.md` 50 332 B · `MANUAL_SMOKE_CHECKS.md` 14 178 B · `audit_cycles/recheck_2026_07_20/REPORT.md` 33 504 B (SHA-256 `55A0BD35…F8FC262` = the value recorded in the registry) · `README.md` 19 189 B · `BRIEF.md` 129 lines. Read-only copies were hash-verified (SHA-256 identical) before analysis. Also consulted read-only: `tests/run_all.js`, `tests/p1_6d_harness.js`, `src/locale.{ru,en,fr,uk}.js`, `assets/text_corrections.json`, `assets/fb2_remake.fb2` (intro quote only). |
| Proof — §1 first sentence (`book_text.md`) | «Вы быстро идете вперед и вскоре оказываетесь в лесу.» |
| Proof — top-level `GD` keys | **1221** (Node script `%TEMP%\gd_schema.js`; contiguous 1…1221, none missing) |
| Proof — first `### ` heading of `REGISTRY_EXCERPT.md` | `### V-01_food_object_pipeline - P0 - DONE` |
| Battery (`node tests\run_all.js`, run once from the repo root) | final line: **`BATTERY: ALL GREEN (16 harnesses + 6 dist checks + baseline)`** — 6d 42, 2C-SHELL 213, `_dist_ui_check` 46 (brief said 42; +4 asserts from the group_81 commit), `verify_reach3.py` 1205 |

---

## 1. VERIFIED-OK

### Block V — the 12 group_80 resolutions (12 checked · 9 matched cleanly · 3 with findings: V-01 partial, X-01 partial, G2-01 not matched)

- **V-01 / R-02 food pipeline — matched (one gap → B-02).** Normalisation sits at the pipeline mouth, before dedupe and the modal: `const offered=ai.items.map(v=>(v&&typeof v==='object'&&v.food)?{kind:'food',id:v.food,stamina:v.stamina}:v);` (game_logic.js:818). Data §389 `{"food":"melon","stamina":4}` ↔ canon «Если хотите, можете съесть сразу. Он добавит 4 ВЫНОСЛИВОСТИ.» `eatFoundItem` is cap-guarded like `eatFood` (`if(S.stamina>=S.staminaMax){showItemNotification([t('vynoslivost_uzhe_polnaya')]);return;}`), marks `_eaten`, logs, notifies, re-renders; `takeItem` refuses `_eaten` (eat-then-take blocked); the update loop targets `.inv-take-btn`. Four slugs `melon/coconut/bun/provisions` present in both `RU_TO_SLUG` and `SLUG_TO_RU`; `syest_srazu` ×4 locales («Съесть сразу» / "Eat now" / «Manger tout de suite» / «З’їсти одразу»). The take-then-eat direction is not guarded — B-02.
- **V-02 mandatory equip — matched.** `if(ai.equip){…}` (line 800) runs before the voluntary offer and cannot be skipped. §71 `{"equip":{"item":"death_of_orcs","swap_out":"whole_sword"}}` ↔ «Оставьте свой меч в кладовой вместо того, который берете с собой»; §1213 `{"equip":{"item":"knight_shield"}}` ↔ «Вы выбираете щит. … (прибавьте себе 1 МАСТЕРСТВО …)» — the +1 is `playerEquipMod()` (`knight_shield` → `m+=1`). Three ui keys ×4 (`ostavlen_vzamen`, `ostavlen_vzamen_2`, `vzyato_v_ruki`). §35 «Можете взять его с собой» stays a voluntary `acquires:"whole_sword"` (second choice). First-visit semantics incl. hash entry: `window.onload` → `renderGame()` → `firstVisit` from `S.visited`.
- **V-03 staged Weakness — matched.** Pickable predicate `const isPickable=(cs.pendingWeakPick&&e.hp>0&&!e.fled);` (1751) makes staged waiters clickable; click sets `cs.weakPickIdx=i` and updates `targetIdx` only when the card is active; red outline `isWeakSel`; round-1 consumption prefers the pick, alive-guarded, falls back to the round target (2609–2612); the debuff is per-enemy `wp.weakDebuff=-2` and is applied through `perMod` only once the waiter is active. Bridges: §116→1175, §655→628, §470→536 (`combat_mod:"ENEMY_WEAK_PICK"`) ↔ canon §116 «уменьшить на 2 СИЛУ УДАРА любого из Орков», §655 «любого из них». §1175's script-managed waiters (`active:false`, no `joins`) are pickable too.
- **V-04 dice persistence — matched.** §781: `S.diceCheckDone[S.section]={a:a,b:b,ok:ok,tgt:tgt}; saveGame();` (2068–2069) precedes `showResolved`; on entry a record short-circuits to the resolved screen. Data `{"dice":2,"gte":10,"win":863,"lose":126}` ↔ «если сумма выпавших очков будет равна 10-ти, 11-ти или 12-ти … если попали, то 863, если нет, — 126». §932: `S.diceLootRoll[S.section]={n:n}; saveGame();` (2129) precedes `showRolled(n)`; Continue without picking commits `S.diceLootDone[S.section]=true; saveGame();` (2087–2088); done → exit only. `normalizeSave` leaves absent records absent (= unrolled), as the resolution states. C10/C11 exist in `MANUAL_SMOKE_CHECKS.md`.
- **X-01 combatResolved — matched for `joins` fights, gap for the §131 script (→ B-03).** `combatResolved(cs)` (= every enemy dead-or-fled) is the only victory test in `combatRound` ×2, `useLarvaInCombat`, `useCopyInCombat`, `useAllyInCombat` and the Death-of-Orcs branch of `startCombat`; `activateStagedJoins` runs after Copy/ally/larva; the bide-time beat `t('protivniki_vyzhidayut')` («⏳ Оставшиеся противники выжидают — вы переводите дух…») and the round button advancing the round while waiting are in place (§388 third knight `joins:{round:11}` ↔ «А через 10 раундов атаки к ним придет на помощь»). §177 lioness `joins:{after_death:0}` now wakes on a Copy kill. The sec1175 script keeps its own management in Copy/ally paths (but not in the larva path — unreachable in play, see B-03). The sec131 script has no management on any non-round kill path — B-03.
- **X-02 condition button — matched.** `updateCombatConditionButtons(cs)`: remove-then-recompute, `if(!cs||!combatState||cs!==combatState)return;` (2520), instance-bound handler (`_bsec`, `_bcs` no-op guard), log dedupe via `cs.condMsgShown`; `clearCombatExtraButtons` also removes `#combat-condition-btn`; called at start/round/end/routed; recomputed after larva/Copy/ally/DoO. §617 `{"target":165,"flee":true,"combat_condition":"enemy_active_1"}` + goblin `joins:{after_death:0}` → with the sword the flee exit appears at fight open.
- **X-03 instant-kill Met — matched.** `endCombat(won)`: `if(ch.combat_condition&&!ch.flee){ S.combatCondMet=S.combatCondMet||{};S.combatCondMet[S.section]=true; }` (2703) for every non-flee condition regardless of kill method; `combatWon` branch of `renderChoices` shows condition choices when `S.combatCondMet[S.section]`; flee-only §617 stays excluded (no phantom escape). §532 «wound_2»→437, §994 →118, §46 `enemy_defeated_1`→98 all covered.
- **X-04 sec.436 — matched.** `fightF.onclick=()=>{ S.sec436_force=false; saveGame(); startCombat(sec.enemies,{...sec, player_attack_mod:1, combat_spells_allowed:[]}); };` (1901); the `if(S.sec436_force){…return true;}` branch precedes the luck button, so the pre-luck flow is unreachable while the flag is set; `normalizeSave` keeps the boolean; §526 `{"target":436}` plain navigation, flag set at the FORCE cast (`useSpell('FORCE'); S.sec436_force=true; saveGame(); goTo(ch.target);`). C12 present.
- **G2-01 weightless weapons — NOT matched (→ B-01).** Registry and literal are as specified (`items.json`: `death_of_orcs/knight_shield/whole_sword` = `"kind":"weapon","slotCost":0`; `const ITEM_SIZES={diving_suit:2,flying_carpet:3,whole_sword:0,death_of_orcs:0,knight_shield:0};` line 1016), and the canon is real: FB2 intro (`assets/fb2_remake.fb2`; not mirrored in `book_text.md`) «Обратите внимание, что деньги и оружие в заплечный мешок не кладутся, и вы можете брать их столько, сколько позволят условия конкретного параграфа.» But the consumer defeats the zero — B-01.
- **R-01 choice index — matched.** `const withIdx=sec.choices.map((ch,idx)=>({ch,idx}));` (2282) feeds the lucky / unlucky / non-luck filters; all four post-luck branches call `makeChoiceBtn(e.ch,false,e.idx)`; `makePurchaseBtn`/`completePurchase` key `shopBought[String(S.section)]` by `choiceIndex`; `makeBatchBtn` keys `S.section+':'+choiceIndex`. `normalizeSave` prunes non-array `shopBought` values, non-finite members (`[null]` gone) and `batchPicked` keys not matching `^\d+:\d+$`. Exploit assessment of the lenient migration: pruning only ever removes unknowable markers; a hand-edited save can re-arm a one-shot purchase regardless of the migration (that is a property of any client-side save, not of the migration) — no new exploit surface beyond deleting an index by hand. §340 choice 6 `{"purchase":true,"gold_cost":1,"grants_items":"wood_piece"}`; §585 idx 0 two coconuts. C13 present.
- **R-02 slug maps — matched (hygiene note → B-05).** All 105 `items.json` keys appear in `SLUG_TO_RU`, all `legacyRu` in `RU_TO_SLUG`; two registry entries share `legacyRu` «Арбуз» (`melon` food, `watermelon` item), producing a duplicate key in the `RU_TO_SLUG` literal that the parity guard cannot see.
- **R-03 label guard — matched at HEAD.** `GAME_RULES.md`: «Текущая редакция соответствует реестру **v2.157**»; the registry's last `version_history` key is `v2.156 -> v2.157` (the excerpt stops at v2.155 because it predates the two later commits). 6d guard green.

### Block X — interactions (19 pairs traced; 15 clean; findings B-02, B-03, B-07, B-08)
- reading flow × post-combat choices: every renderer writes into `#c-list` (`renderChoices` incl. `combatWon`, `renderRiddle`, `renderDiceRoll/Check/Loot`, `renderStakePicker`, shop re-render via `renderChoices(sec)` after `completePurchase`) — clean.
- dialog controller × combat modal: `#modal-combat` has no `[onclick*="closeModal("]` control → Escape no-op; initial focus = first focusable (`Удар!`); the new open-order stack (`_bcDialogStack`) gives z-index by open order — clean by code reading.
- sheets × item-offer modal: `#modal-inventory` opens via the `on` class → pushed on the stack above `#overlay-sheet` (`zIndex=100+n`) — reachable; clean.
- HUD sync × stat mutations: every path that changes `S.stamina/gold/skill/luck/flask` ends in `updateHUD()` (wrapped by `syncHudBar`): combatRound, spells, larva, Copy/ally, healing (both), betting, purchase, gold cost, eat/eatFound, flask, dice bash, auto_items (deferred to `closeInvModal` while the offer modal is open) — clean.
- hash entry `#N` × sheet/HUD: `window.onload` → `S.section=parseInt(h)` → `renderGame()` → `updateHUD()`; no sheet is open at load — clean.
- F5 windows closed by group_80: §436 (flag persisted, consumed on click), §340/§585 (indices real, pruning sound), §781/§932 (records saved before display) — clean by save round-trip reasoning. NOT closed and not in scope of group_80: the plain luck check and combat state — B-07, B-08.
- autosave note × reload: `lastAutosaveAt` is a runtime `var`, not in `S` → empty after reload, by design.
- reduced-motion × `animationend`: no `animationend` listener in the engine (item notifications use `setTimeout`) — clean.
- language switch × combat modal / sheets: `repaintAfterLangSwitch` → `renderGame({repaint:true})` re-renders behind the open modal (no auto_items re-fire, betting skipped) and calls `saveGame()`; card names in the open modal keep the old language (cosmetic) — clean apart from the mid-combat save noted in B-08.
- `pickup_batch` × index plumbing: `makeBatchBtn(ch, e.idx)` in all branches — clean.
- Death of Orcs × staged joins (§617, §628) — activation via `activateStagedJoins` after the open-kill — clean; × §1175 script — see B-03 (unreachable in play).
- Weakness pick × Copy/larva before round 1 — alive-guarded fallback — clean.
- deadline × bide-time: the top-of-round deadline check requires active enemies, so waiting rounds never trip a deadline; the four deadline fights have no `joins` — empty intersection, clean.

### Block G2 — residual reverse search (198 candidates · 14 residual · 0 new)
Automated scan of all 1221 paragraphs for imperative orders («вычт-/прибав-/отним-/уменьш-/потеря-/восстанов-/добав-/только если/не успе-/за N раундов/действ-» + a stat/gold/round noun) gave 198 paragraphs; 184 carry a GD mechanic. The 14 without any mechanic were inspected: §§45, 59, 284, 305, 324, 330, 511, 524, 714, 767, 831, 996, 1023 are narrative uses of «лишь / вернуть / действие / верно / не успевают / не слишком удачен / затерялся»; §542 «Когда будете драться мечом Зеленого рыцаря, то прибавляйте себе каждый раз 1 МАСТЕРСТВО» is wired upstream (§553 `acquires:"whole_sword"` → `playerEquipMod`). No canon order provably unwired and absent from §4–5 was found.

### Block R — regression A–F (census at HEAD; data identical to `459d8a1`)
paragraphs **1221** · navigation edges **2218** · `auto_items` **179** · combat pids **76** / enemy entries **120** / multi-enemy **24** · `damage` overrides 7 · `joins` 9 · spell hooks **100** + **3** `spell_any` · `inventory_condition` **131** · `acquires` **15** · `gold_condition` **34** · `gold_cost` **59** (brief says 58 — the brief is the outlier; `GAME_RULES.md` §8 also says 59: 36 on `purchase` choices + 23 on navigation choices) · `purchase` **36** · `pickup_batch` **5** · riddles **7** · dice sections **6** (4 `dice_roll` + 1 `dice_check` + 1 `dice_loot`) · `round_deadline` **4** · `player_attack_mod` **10** · `combat_condition` **4** · `combat_mod` 14 · `consume_on_use` 32 · items.json **105** (food 20, weapon 3, item 76, flag 6) · ui keys **334 ×4** with full RU/EN/FR/UK parity (brief: 331 at `459d8a1`; +3 `ui_aria_*` from group_81) · enemies 66 / spells 8 / allies 2 / map 43 / p 1221 per locale · EN/FR ui values with Cyrillic: 0 · FR ASCII apostrophes in ui: 0 · guillemet NBSP: 0 issues · FR NNBSP/NBSP after stripping inline HTML/CSS: **1** violation (B-04) · 6d guards 42 · 2C-SHELL 213 · `_dist_ui_check` 46 · BFS reachable 1205. README census lines (179 `auto_items`, 1221) current. `MANUAL_SMOKE_CHECKS.md` C10–C16 present.

---

## 2. FINDINGS

| id | block | severity | §§ / keys / selectors | evidence (exact code or Russian quote; resolution quoted where disagreeing) | suggested minimal fix |
|---|---|---|---|---|---|
| **B-01** | V (G2-01, V-02) | **P0** | `getItemSize()` game_logic.js:1017–1020; `ITEM_SIZES` :1016; `getBagUsed()`; §35, §71, §1213 | **Verified by code reading (and by executing the two lines in isolation).** `return ITEM_SIZES[canonItem(name)]||1;` — for the three weapons the lookup yields `0`, and `0||1` is `1`, so `getItemSize('whole_sword')`, `getItemSize('death_of_orcs')` and `getItemSize('knight_shield')` all return **1**; a bag with the two swords and the shield reports `getBagUsed()=3`. This contradicts the G2-01 resolution — «both swords and the shield now occupy zero of the seven slots and canonical pickups can no longer be starved by armament» — and the V-02 resolution's «being weightless now, can never be blocked by a full bag either» (§35 «Можете взять его с собой» goes through `applyChoiceAcquires` → `showInventoryModal`, whose Take button is disabled at `getBagUsed()+getItemSize(item)>getBagSize()`). The battery stays green because `p1_6d_harness.js` §9j asserts the **literal** (`sizes.includes(k+':0')`), never the function's return. The Gemini track-B run's «Weapons (slotCost: 0) bypass standard inventory capacity limits» is therefore wrong by code reading. Side effect: `S.inventory.push(eq.item)` in the equip branch is unconditional, so at a full bag §71/§1213 push the bag to 8/7 (harmless only if weapons were truly weightless). | `function getItemSize(name){ if(!name) return 1; const v=ITEM_SIZES[canonItem(name)]; return (typeof v==='number')?v:1; }`; change the 6d guard to call `getItemSize()` on the three weapons (expect 0) instead of grepping the literal; document the armament class in `GAME_RULES.md` §1/§6 (currently «заплечный мешок (7 слотов; diving_suit занимает 2, flying_carpet 3)» — no mention of `slotCost:0`). |
| **B-02** | X (V-01 × offer modal) | **P1** | `renderInvModalCurrent()` :458–492 (branch at :485), `eatFoundItem()` :494–508, `takeItem()` :510–520; §389 and every `auto_items.items` food offer | **Verified by code reading.** In the "already in bag" branch only the take button is disabled: `btn.textContent=t('v_meshke_2');btn.disabled=true;btn.style.opacity='.4';` — the `#inv-eatnow-N` button is disabled only in the `_eaten` branch (`if(item&&item._eaten){ const eb=document.getElementById('inv-eatnow-'+i); if(eb){eb.disabled=true;…}`). `eatFoundItem` guards only `if(!item||typeof item!=='object'||item.kind!=='food'||item._eaten)return;` — not bag presence. `takeItem` pushes the same object reference (`S.inventory.push(item);`). Sequence Take → «Съесть сразу» therefore grants `+stamina` **and** leaves the food in the bag (the inventory entry is persisted as `{"kind":"food","id":"melon","stamina":4,"_eaten":true}`), after which `eatFood()` grants it again: one canon arbuz = two servings. The resolution only promises «no eat-then-take duplicate»; the reverse order is open. | In the "already in bag" branch also disable `#inv-eatnow-N`; and in `eatFoundItem` add `if(S.inventory.some(it=>canonItem(it)===canonItem(item)))return;`. Optionally strip `_eaten` when pushing to the bag. |
| **B-03** | X (X-01 × `combat_script`) | **P0** | §131 `sec131_eagle_joins` (`combatRound` :2574–2593 and :2642–2649; `useCopyInCombat` :2807–2880; `useLarvaInCombat` :2782–2805; `useAllyInCombat` :2897–2967); `handleCanonCombatMilestones` :1952 (dead); GAME_RULES §3.7 | **Verified by code reading; reachable in normal play.** The eagle is script-managed: `active:false` from `startCombat` (`script==='sec131_eagle_joins' && idx>0`) and has **no `joins`** (GAME_RULES §3.7: «Прецедентные скрипты §131/§1175 не переведены на joins намеренно»). Its two activation sites are (a) the top-of-round block gated on the goblin being alive — `if(cs.round===5 && first.hp>0 && !cs.special.reinforcementsJoined)` — and (b) the after-strike block `if(first.hp<=0 && !cs.special.reinforcementsJoined){ cs.enemies[1].active=true; … }` (:2644), which sits **after** `if(alive.length===0){ if(combatResolved(cs)){endCombat(true);} else{ log 'protivniki_vyzhidayut' … } return; }` (:2586–2593). Unlike the sec1175 script, none of `useCopyInCombat`, `useLarvaInCombat`, `useAllyInCombat` carries a sec131 block, and `activateStagedJoins` ignores enemies without `joins`. So when the goblin is felled by a non-round kill (Copy side-fight, a §932 larva — path §1123→…→§131 exists, BFS — or the bell's bear; the amulet is inert inside the castle and §131 ∈ `CASTLE_SECTIONS`), `combatResolved` is false (eagle hp 7), every further «Удар!» returns at :2593 with «⏳ Оставшиеся противники выжидают…», the eagle never activates and the fight can neither be won nor left (no flee choice at §131). Before v2.146 the same sequence ended in a premature "victory" (the X-01 defect); the fix turned it into a soft-lock. Canon requires the eagle: «Через 4 раунда атаки к нему присоединяется Орел-часовой … Если вы победили обоих стражей». The same shape exists at §1175 for the larva path and for Death-of-Orcs at fight open (the first orc dies at open, the script's first-death block is never reached), but BFS shows no path §755→§1175 and none §1123→§1175, so those variants need a hand-typed item (`addItem` → «Меч «Смерть Орков»») or an edited save. `handleCanonCombatMilestones(cs)` (the §1175 re-check helper) is defined but never called. | Run the script milestone checks from every non-round kill path and from the waiting branch: e.g. add a `handleScriptedJoins(cs)` covering sec131 (goblin dead → eagle active) and sec1175 (first orc dead → orcs 2–3 active + luck prompt) and call it in `useCopyInCombat`, `useAllyInCombat`, `useLarvaInCombat`, the DoO branch of `startCombat`, and at the top of `combatRound` **before** the `alive.length===0` return; or give the eagle `joins:{round:5,after_death:0}` and let `activateStagedJoins` own it. Add a 6d scenario "§131 Copy kills goblin → eagle active". |
| **B-04** | R (i18n) | **P2** | `ui_autosave_note` (FR), `src/locale.fr.js`; renders in `#autosave-note` | **Verified by code reading.** FR value `"Sauvegarde auto :"` has U+0020 before the colon; the brief requires NBSP before `:` on new keys and every other FR colon key follows it («Mangé : », «Total : », «Maintenant : », «TROUVÉ :» all use U+00A0). After stripping inline HTML/CSS this is the only FR ui value violating the house rule. | `"Sauvegarde auto\u00a0:"`. |
| **B-05** | R (registry hygiene) | **P2** | `RU_TO_SLUG` literal :947 and :976; `items.json` `melon` / `watermelon`; §300 `auto_items.items:["watermelon"]`, §389 `{"food":"melon"}` | **Verified by code reading.** Two registry entries share `legacyRu` «Арбуз»: `"melon":{"legacyRu":"Арбуз","kind":"food"}` and `"watermelon":{"legacyRu":"Арбуз","kind":"item"}`; the generated literal therefore contains `"Арбуз":"melon"` (line 947) and `"Арбуз":"watermelon"` (line 976) — the later key wins, so `canonItem("Арбуз")` → `watermelon`. Consequence: a legacy RU string «Арбуз» in an old save or typed into the add-item field never dedupes against the §389 melon food object, and both display as «Арбуз». The R-02 guard (every `legacyRu` present in the literal) is blind to duplicate keys. | Give the item a distinct `legacyRu` (e.g. «Арбуз (второй)») or merge §300 onto the food entry; extend the 6d parity guard with a "no duplicate `legacyRu`" assert. |
| **B-06** | R (docs) | **P2** | `GAME_RULES.md` §3.7, §3.8, §1/§6; `BRIEF.md` §6 census | **Verified by reading.** §3.7 lists the activation sites as «активация в `activateStagedJoins` (обе alive-проверки + убийства личинкой + «Смерть Орков»)» — after v2.146 Copy and ally kills also activate joins; §3.8 says the targeted Weakness «ложится на текущую выбранную цель («любого из них» = клик игрока до первого удара)» — after v2.147 it lands on `cs.weakPickIdx` (staged waiters selectable), falling back to the round target; §1/§6 do not mention the `slotCost:0` armament class (see B-01). The brief's census `gold_cost 58` is a typo (data and GAME_RULES: 59). | Refresh the three sentences at the next label bump; fix the brief's number in the archived copy's errata. |
| **B-07** | X (F5 window, pre-existing) | **P1** | `doLuckCheck` :3040–3103; `let luckDone={}; let luckResult={};` :3015–3016; `goTo` :2319; the seven one-sided-luck paragraphs of GAME_RULES §3.5 (§§203, 289, 377, 418, 421, 1186 + scripted §436) | **Verified by code reading; suspected impact; not a group_80 regression (the previous cycle only flagged the dice windows).** The luck decrement is saved (`S.luck=Math.max(0,S.luck-1); … updateHUD();saveGame();`) but the outcome lives only in the runtime globals `luckResult[S.section]='unlucky'` / `luckDone[S.section]=true`, which `goTo` and every reload reset. After an unlucky roll the death is applied only when «Конец приключения» is clicked (`btn.onclick=()=>{…showDeathOverlay();}`); an F5 before that click re-renders the paragraph with `hasPendingLuck` true and offers the check again (at one luck point per attempt). This is the same defect class the V-04 resolution treated as P0 for §781 («the mandatory check can no longer be rerolled to success»), left open for ordinary luck checks. | Persist the resolved roll like §781: `S.luckDone[section]={roll,lucky}` + `saveGame()` before rendering the panel; restore on entry; (for the fatal-unlucky case, either commit death or keep the resolved panel). |
| **B-08** | X (F5 window, pre-existing design) | **P2** | `combatState` (runtime only), `combatDone` :2325, `combatRound` (no `saveGame`), `useSpell` (saves), `useLarvaInCombat` (saves), `repaintAfterLangSwitch` (saves) | **Verified by code reading; design note.** Combat is never persisted, so F5 mid-fight restarts the paragraph with full enemy HP — but the hero's stamina is persisted **only** when something else saved during the fight (`useSpell` → `saveGame()`, larva, healing, a language switch → `renderGame({repaint:true})` → `saveGame()`): after a Force/Weakness cast the reload keeps the wounds and loses the charge; without a cast it restores pre-fight stamina (a free retry). At §436 an F5 during the +1 fight lands on the pre-luck flow with the paid Force gone — consistent with the X-04 wording «commits atomically with the fight it pays for» but worth a MANUAL_SMOKE_CHECKS note. | Either snapshot `combatState` into `S` on every round (restore on load) or make the mid-combat behaviour uniform (never save during a fight, or always). Document the chosen rule in GAME_RULES §2. |

All rows are hypotheses; B-01 and B-03 are the two I would re-verify first.

---

## 3. COUNTS

- **P0: 2** (B-01 weapons not weightless; B-03 §131 soft-lock) · **P1: 2** (B-02 take-then-eat double serving; B-07 luck-check F5 reroll) · **P2: 4** (B-04, B-05, B-06, B-08).
- **Single highest-confidence finding: B-01** — `ITEM_SIZES[...]||1` evaluates to 1 for every `0` entry; demonstrated by executing the function in isolation (`getItemSize('whole_sword')=1`, three weapons = 3 slots), and the reason the green 6d guard did not catch it is visible in `p1_6d_harness.js` §9j (literal check only).
- Volumes: V 12/12 resolutions traced (9 clean) · X 19 pairs (15 clean) · G2 198 candidates / 14 residual / 0 new · R full census reconciled with one brief typo · battery ALL GREEN quoted above.

---

## 4. NOT-CHECKED

- No browser run, no DOM emulation, no localStorage round-trip was executed; all F5/reload statements are save round-trip reasoning by code reading. `build.sh` was not run; `dist/` was only exercised through the six `_dist_*_check.js` scripts inside the battery.
- Track A items (UI/CSS/fonts/a11y) were not audited; the dialog-controller and sheet remarks in block X are limited to their interaction with mechanics. The group_81 diff was read only to confirm it changes no mechanic.
- The FB2 intro rule for G2-01 is not present in `book_text.md`; it was quoted from `assets/fb2_remake.fb2` (repository file, read-only). The in-game preface/pregame texts do not contain the bag/weapon rules at all — whether that is intended is not determinable from the provided files.
- Reachability statements in B-03 come from a BFS over `GD` choice/riddle/dice/deadline targets that ignores `inventory_condition`/`gold_condition`/spell gating; they are therefore upper bounds ("a path exists in the graph"), and the negative statements ("no path §755→§1175, §1123→§1175") are exact for the graph as encoded.
- The sec1175 × larva and sec1175 × Death-of-Orcs variants of B-03 were not traced beyond the graph argument; whether `addItem` free text is considered a supported entry point is a design question for the maintainer.
- Overlap with the archived Gemini track-B report was checked only by keyword (no `getItemSize`/`||1`, no §131, no eat-now mention there); its own findings were not re-adjudicated.
- Combat balance, art, sound, PWA, and the 127 smoke routes were out of scope and untouched.
