# REPORT — Track A (UI / accessibility re-audit) — recheck cycle 2026-09
**Auditor:** Claude (Fable 5.1) via Windows MCP, full repository access, read-only. **Date:** 2026-09-04.
**Audited state:** `main` @ **`79124a5`** (registry v2.157). The brief names `459d8a1` / v2.155; HEAD is two commits later — `0dfb2ac` (cycle docs, group_81 opened) and `79124a5` (group_81 batch 1: UA-01..UA-04). Everything below is checked against HEAD.
**Mode:** every claim is a hypothesis "by code reading" unless marked otherwise. Two extra evidence classes were used and are labelled explicitly: (a) `node tests\run_all.js` executed once, as instructed; (b) a **jsdom simulation** of a COPY of `dist\podzemelye-chyornogo-zamka-remake.html` in the auditor sandbox (DOM structure only — jsdom has no layout, so focus targets, `offsetParent`, painting and safe areas were NOT exercised; one selector, `[onclick*="closeModal("]`, is also not matched by jsdom's nwsapi engine although it is valid CSS in browsers — the Escape path therefore stays code-reading-only). Nothing in the repository was created, modified or deleted; the only write is this file in the git-ignored `_handoff\audit_2026_09_claude\`.

---

## 0. PRE-FLIGHT

1. Files read (repository paths, sizes from `Get-Item`): `src\game_shell_top.html` 66,857 B (840 lines) · `src\mobile.css` 7,440 B · `src\fonts\fonts.css` 4,793 B · `src\game_logic.js` 172,938 B (3,305 lines) · `src\map_module.js` 36,672 B · `MANUAL_SMOKE_CHECKS.md` 14,178 B · `audit_cycles\recheck_2026_09\REGISTRY_EXCERPT.md` 50,332 B · `audit_cycles\full_audit_2026_07_14\UI_AUDIT.md` 21,341 B · `README.md` 19,189 B · `audit_cycles\recheck_2026_09\BRIEF.md` (read in full first) · `assets\text_corrections.json` 651,775 B (group_81 read directly, as instructed) · additionally `src\fonts\Forum-cyr.woff2` 16,528 B, `src\fonts\OFL.txt` 4,399 B, `src\locale.{ru,en,fr,uk}.js` (key parity), `build.sh` (read only, never run), `dist\podzemelye-chyornogo-zamka-remake.html` 3,698,035 B. The eight Track A files copied to the sandbox for line-numbered reading were SHA-256-verified identical to the repository copies (e.g. `game_logic.js` `cf7aafea…7219`, `game_shell_top.html` `23998226…1fc6`).
2. Git: `git rev-parse --short HEAD` → `79124a5` (expected `79124a5` ✓); `git status --short` → empty ✓; `git log --oneline -5`: `79124a5 fix(a11y): group_81 batch 1 - dialog open-order stack, keyboard-operable inventory, combat round status, emoji-free HUD aria` / `0dfb2ac docs(audit): recheck 2026-09 - …` / `a051c30` / `459d8a1` / `f8f6812`.
3. Proof of access (verbatim):
   - `src\game_shell_top.html:815` — `<h2 id="menu-modal-title" data-i18n="ui_menu_title">Меню</h2>`
   - `src\game_logic.js:119` — `// >>> BC_A11Y_DIALOGS (UI-04, group_79): shared dialog controller >>>`
   - `REGISTRY_EXCERPT.md:80` (first `### UI-` heading) — `### UI-01_choices_scroll_architecture - P0 - DONE`
4. Battery: `node tests\run_all.js` from the repository root (Node v26.8.1), exit 0, final line: **`BATTERY: ALL GREEN (16 harnesses + 6 dist checks + baseline)`**. `_dist_ui_check.js` prints `46 passed` (brief census 42 at `459d8a1`; +4 asserts shipped with group_81 batch 1 per the v2.156→v2.157 ledger entry).
5. group_81 (`pending_corrections.group_81_2026_09_04_recheck`, ledger v2.157): UA-01 `_bcDialogStack` + inline `z-index 100+depth` (logic.js 126, 136–137, 150–151) — matches resolution; UA-02 `.inv-eat`/`.inv-remove` rendered as `<button type="button" … aria-label="…" title="…">` (logic.js 923–924) + CSS reset (shell 198–199) — matches; UA-03 `#combat-round-status` `.sr-only role="status" aria-live="polite"` (shell 705) fed by the child-count diffing mirror (logic.js 215–234) — matches (one edge, see CA-06); UA-04 `ui_aria_map/ui_aria_log/ui_aria_menu` on the HUD buttons (shell 603–605), present in all four locales — matches. All four re-verified as DONE.

---

## 1. VERIFIED-OK (per block, with volumes)

**U1 Dialog controller (BC_A11Y_DIALOGS, logic.js 119–180) — 9 items checked, 8 matched, 1 finding (CA-03).**
`_bcTopDialog()` returns the topmost stack entry that is still `.on` and in the DOM, DOM-order query only as fallback (129–132). `_bcDialogOpened`: role/aria-modal, stack push + `zIndex=100+depth` for `.modal-overlay` only (`.end-overlay` keeps 200), `aria-labelledby` derived from `h1,h2,h3,.modal-title,.map-title,.end-title` only when neither `aria-labelledby` nor `aria-label` is present (all seven static overlays carry ids in markup), opener recorded only if `activeElement` is not body and not inside the dialog, initial focus = first of `button,select,input,textarea,a[href],[tabindex]` filtered by `!disabled && tabIndex>=0 && aria-hidden!=='true' && offsetParent!==null`, else the overlay itself with `tabindex=-1`. Initial-focus targets by markup order: combat → `#btn-combat-round` («Удар!»), luck → `#btn-luck-roll`, offer → first «Съесть сразу»/«Взять», map → `#map-layer-select`, menu → the language `<select>`, sheet → `.sheet-close`, death/win → the restart button. Escape gating: `top.querySelector('[onclick*="closeModal("]')` — present only in `#overlay-map` («Закрыть (M)»), `#overlay-menu` («← Закрыть»), `#overlay-sheet` («✕»); combat / luck / `#modal-inventory` (`closeInvModal()`) / death / win (`location.reload()`) are not dismissible ✓. Tab/Shift+Tab wrap at first/last and when focus is outside the top dialog (174–177) ✓. Two-dialog case (sheet + `M`): simulated on dist — stack `overlay-sheet,overlay-map`, map `z=102` over sheet `z=101`, top = map; after closing the map top = sheet; after closing the sheet stack = 0 and inline z cleared ✓. Observer scope: `document.body`, `attributes`, `attributeFilter:['class']`, `subtree` — 78 class-mutation sites in the engine, callback does one `classList` test per record; combat re-renders use `innerHTML` (childList, not observed) → cost negligible by code reading ✓.

**U2 Keyboard reachability / focus visibility — 21 controls checked, 15 OK, findings CA-02, CA-07, CA-08, CA-09, CA-17, CA-18.**
`outline:none` resets with specificity: `.btn` (0,1,0) shell 82; `.cr-name input` (0,1,1) 104; `.sb-notes textarea` (0,1,1) 206; `.add-item-input` (0,1,0) 320; `#riddle-input` (1,0,0) 424. The ring rule (shell 471) `button:focus-visible,.btn:focus-visible,.choice-btn:focus-visible,select:focus-visible,input:focus-visible,textarea:focus-visible,[tabindex]:focus-visible` beats or ties-and-follows all of them except `#riddle-input` (CA-18). Reachable: `#inv-add-btn` (`role="button" tabindex="0"` + inline Enter/Space `onkeydown` with `preventDefault`), `.map-mini-card` (same pattern), six HUD `<button type="button">`, `.sheet-close`, map `<select>` + four `<button type="button">`, riddle `<input>` + `<button>` (rendering itself broken, CA-01), dice widgets (`<button class="choice-btn">`), choice/purchase/batch/bash buttons (`document.createElement('button')`), spell +/- (`<button class="qty-btn" aria-label=… disabled>`), luck buttons, visual-dock pills (`<button type="button">`), offer-modal take/eat/drop (`<button>`), event-log close/clear (`<button>`), hero-name `<label for="hero-name">`.

**U3 Live regions — 5 channels checked, 3 OK, findings CA-04, CA-05, CA-06.**
`#luck-result aria-live="polite"` (shell 725) written by `doLuckCheck` via `formatLuckPanel` ✓; `#spell-counter-chip aria-live="polite"` ✓; `#autosave-note aria-live="polite"` ✓; `#combat-round-status` mirror: one round announces e.g. `— Раунд 1 — · Вы: 2к6(6) + 10 = 16 · ТОРГОВЕЦ: 2к6(8) + 7 = 15 · → Вы ранили ТОРГОВЕЦ (−2 вын., осталось 15)` (simulated on dist, §36) — concise and sufficient as the minimal combat channel; combat/event logs deliberately non-live (do-not-re-flag) ✓.

**U4 Responsive / safe area — 14 items checked, 10 OK, findings CA-10, CA-11.**
`viewport-fit=cover` (shell 5) ✓; `.scr,.modal-overlay{padding-top/right/left: var(--safe-*)}` and `.end-overlay{calc(40px + …)}` (mobile.css 34–35) ✓; `.item-notification` top/right insets (shell 348) ✓; `.story-area` bottom `calc(32px + var(--safe-bottom,0px))` / mobile `calc(24px + …)` ✓; `.sheet{padding-bottom:var(--safe-bottom,0px)}` ✓; `.visual-dock` bottom inset ✓; dvh fallbacks on `.modal` (100vh/100dvh), `.map-modal-shell`, `.sheet` (82vh/82dvh), sheet textarea (40vh/40dvh), title rider (52vh/52dvh) ✓; `@media (max-height:600px)` title rule with `margin:auto`, `justify-content:flex-start`, `overflow-y:auto` (mobile.css 242–248) ✓; `clamp(20px,5vw,40px) clamp(16px,7vw,60px)` on preface/pregame (shell 567, 579) ✓; breakpoint split `≤700` (HUD, both orientations) / `701–900 landscape` (sidebar 260) / `701–900 portrait` (sidebar 260) / `>900` desktop — every width has a rule set ✓.

**U5 Reading flow (UI-11 A) — 9 items checked, 8 OK, finding CA-01 (P0).**
`.reader{max-width:72ch;margin:0 auto;font-family:var(--font-text);font-size:25px;}` (shell 227) — `ch` is computed from the wrapper's own Cormorant Garamond 25px face ✓; markup order `#s-num` → `#s-text` → `#c-area` («Ваш выбор», `data-i18n="ui_your_choice"`) → `#c-list` inside `.reader` inside the `#s-area` scroller (686–694) ✓; scroll reset `document.getElementById('s-area').scrollTop=0` on every paragraph (logic.js 727) ✓; long lists (§132/§340) are plain buttons in one scroll ✓; `body.hide-inline-art .illustration-container{display:none!important}` and the illustration is emitted inside `#s-text` (716/723), i.e. inside the capped column with `max-width:100%` ✓; renderers writing to `#c-list`: `renderDiceRoll` 2015, `renderDiceCheck` 2041, `renderDiceLoot` 2078, `renderStakePicker` 2201, `renderChoices` 2231 (post-combat and shop re-renders go through `renderChoices`) ✓ — `renderRiddle` does NOT (CA-01). Heading fit: sensible for choices/shop/post-combat/stake picker; semantically loose but harmless for the dice widgets (a single «Бросить кубик» action under «Ваш выбор»).

**U6 Phone HUD + sheets (UI-03 A) — 11 items checked, 9 OK, findings CA-12, CA-13.**
Reparenting: `openSheet` records `{node,parent,next}` then `appendChild`; `returnSheetSection` re-inserts before the recorded `next` when it still belongs to the original parent (logic.js 192–204); simulated round-trip for `inv` (sb-flask+sb-inv), `spells`, `notes` restored the exact sidebar order every time ✓. `.sheet-body .sb-section-title{display:none}` ✓; title snapshot from the first `.sb-section-title` (for `inv` = «Заплечный мешок (0/7)», the flask has none) — by design ✓. `syncHudBar` wrapper installed at 206 over the hoisted declaration; every `updateHUD()` call in the engine resolves through the global binding; `map_module.js` never calls `updateHUD`; simulated `S.stamina=7; updateHUD()` → `hb-stamina` "7/20" ✓. `showScr` hides the FAB when `isMobileHud()` (115) and the `matchMedia('(max-width:700px)')` change listener closes the sheet + toggles the FAB (207–211) ✓. The flask (`#sb-flask`) travels with the bag sheet ✓. `M` with a sheet open → map on top of the sheet (UA-01 stack) ✓. Language switch with a sheet open: section title span updates («Backpack»), close `aria-label` updates («Close»), `#sheet-title` keeps the snapshot (by design) — and the switch is not reachable while a sheet is open anyway (the menu is under the overlay, Tab-trapped) ✓. HUD buttons 36 px = the resolution's stated size (WCAG 2.5.8 minimum 24 px) ✓.

**U7 Fonts (UI-07 B) — 8 items checked, 6 OK, findings CA-14, CA-15.**
`fonts.css` Forum block `unicode-range: U+0301, U+0400-045F, U+0490-0491, U+04B0-04B1, U+2116` covers є/ї/ґ/і/№; the woff2 itself (fontTools cmap read of `Forum-cyr.woff2`, 103 mappings) contains U+0404/0454/0406/0456/0407/0457/0490/0491/2116/0301 and 95/96 of U+0400–045F ✓. Chains `--font-ui:'Cinzel','Forum','Cormorant Garamond',serif` / `--font-title:'Cinzel Decorative','Forum','Cormorant Garamond',serif` (shell 19–20) match the resolution; per-glyph fallback: Latin → Cinzel (Latin-only range), Cyrillic → Forum, anything else → Cormorant → `serif` ✓. `font-display:swap` on every face; `scripts/build_shell.py` (per `build.sh` 8–11) strips the `fonts.googleapis.com` `@import` and embeds the woff2 as base64 — dist contains only the comment mention of googleapis and `src: url('data:font/woff2;base64,…')` for Forum, so no network fetch and no visible swap flash ✓. Removed faces: zero occurrences of `Veles` / `Old Face` in `src/`, README, PROJECT_NOTES and dist ✓. `.end-title` 42 px with `†`/`✦` glyphs: `†` (U+2020) is inside the Cinzel Decorative range, `✦` (U+2726) falls to `serif` — acceptable.

**U8 i18n channels — 6 items checked, 5 OK, finding CA-16.**
`applyStaticI18n` (logic.js 677–687) handles `data-i18n` (textContent), `data-i18n-html`, `data-i18n-ph`, `data-i18n-title`, `data-i18n-aria` + `document.title` + `<html lang>` ✓. Parity of 24 relevant keys (`ui_hud_toolbar, ui_btn_close, ui_aria_map/log/menu, ui_lbl_spells/bag/notes/map, ui_btn_log/menu, ui_aria_map_layer/zoom_in/zoom_out, ui_map_topbar_hint, ui_autosave_note, ui_your_choice, ui_title_add_item, ui_ph_add_item, ui_title_map_open, ui_btn_map_close, vybrosit, syest, vyn`) present ×4 ✓. HUD aria: `ui_lbl_spells`/`ui_lbl_bag`/`ui_lbl_notes` are emoji-free in all four locales; the emoji keys `ui_lbl_map` («🗺 Карта»), `ui_btn_log`, `ui_btn_menu` are no longer used as aria names anywhere ✓ (known P2 resolved by UA-04). Map topbar: `updateMapTopbar` overwrites `#map-topbar-layer` (`t('sloy')+layer.title`) and `#map-topbar-status` on every `renderMapSvg`; `repaintAfterLangSwitch` runs `applyStaticI18n()` before `bcRefreshMapLanguage` (logic.js 607–611), which re-renders when the overlay is open ✓. Autosave note = `t('ui_autosave_note')+' §'+S.section+' · HH:MM'` → RU «Автосохранение: §12 · 14:05», EN «Autosaved: §12 · 14:05», UK «Автозбереження: §12 · 14:05», FR «Sauvegarde auto : §12 · 14:05» (see CA-16 for the FR space).

**U9 Regression of UI-01/02/05/08 — 4 items, 4 OK.**
UI-01: the quick-fix cap is retired — `.choices-area{position:relative;z-index:1;margin-top:28px;padding:22px 0 8px;border-top:1px solid var(--border);}` (shell 256), no `max-height`/`overscroll-behavior` in shell or mobile.css ✓ (superseded by UI-11 A as recorded). UI-02: all twelve combat selectors and `.visual-dock/.visual-pill/.on` styled (shell 237–255), `body.hide-inline-art` rule present ✓. UI-05: `--muted:#8a7a9e`, `--red2:#d6574b`, `--tap:44px` (shell 14–18); mobile touch block uses `var(--tap,44px)` on `.inv-remove,.inv-eat,.event-log-close,.map-controls button,.qty-btn` (mobile.css 213–221); `.qty-btn{width:var(--tap);height:var(--tap)}` ✓. UI-08: `renderSpellSel` local `tot` (no `t` shadowing), `#spell-counter-chip aria-live="polite"` written each render, `#slots-bar aria-hidden="true"`, +/- with `aria-label="${t('aria_ubrat')}…"` / `aria_dobavit`, real `disabled` at both bounds, 44×44 ✓.

---

## 2. FINDINGS

Every row is a hypothesis. "verified by code reading" = deterministic from the quoted code; "+ simulated" = also reproduced with the jsdom copy of dist; "suspected" = depends on runtime/AT behaviour I could not exercise.

| id | block | sev | §§ / keys / selectors | evidence (exact code or Russian quote; resolution quoted where I disagree) | suggested minimal fix |
|---|---|---|---|---|---|
| **CA-01** | U5 | **P0** | `renderRiddle` (logic.js 1349–1409, container lookup 1352–1353); riddle paragraphs §439, §67, §95, §1113, §1131, §992, §435 (`GD[n].riddle`, `choices:[]`) | `const cont=document.getElementById('choices');` / `if(!cont)return;` — no element with id `choices` exists in `game_shell_top.html` (the reading column is `#c-area` / `#c-list`, shell 690–692) nor in dist; `git log -S'id="choices"'` over the whole history returns nothing, and the introducing commit `b6cd0cb` (2026-05-18) added only riddle CSS to the shell. The other five renderers use `getElementById('c-list')` (2015, 2041, 2078, 2201, 2231). Consequence: `renderGame` (730) dispatches `renderRiddle`, which returns before clearing `#c-list`, so the **previous** paragraph's buttons stay on screen and no input/«Ответить»/exit button is rendered. **Simulated on the built dist:** at §1131, §992, §439, §67, §95, §1113 and §435 `#riddle-input` = absent and `#c-list` still held §1's two buttons, first = «По правой дороге (1219)». No harness references `renderRiddle`; `SMOKE_TEST_PATHS.md` only asks for a manual check («sec.1131 … should render the riddle widget»). Verified by code reading + simulated. | Change line 1352 to `document.getElementById('c-list')` (and clear it as the siblings do). Add a `_dist_ui_check`/2C-SHELL assert that every id passed to `getElementById(` by the five `#c-list` renderers exists in the shell, and a MANUAL_SMOKE_CHECKS row for §1131. |
| **CA-02** | U2 | **P1** | `.combat-enemy` cards, `updateCombatEnemyDisplay` (logic.js 1754–1761), `startCombat` markup 2358–2374; 24 multi-enemy combats; V-03 Weakness pick (§1175/§628/§536) | Cards are `<div class="combat-enemy">` with `card.onclick=((isTargetable&&multiTarget)||isPickable)?()=>{…}:null;` and selection shown only by `card.style.outline=isWeakSel?'2px solid var(--red2)':(isSel?'2px solid var(--gold)':'none');` — no `tabindex`, `role`, keydown or `aria-pressed`; simulated on dist (§36): `tabindex=null role=null`. G-05 per-round target selection and the V-03 Weakness pick are therefore pointer-only (WCAG 2.1.1 on the primary combat flow; screen-reader users also get no selected state). The UI-04 resolution deferred «Real <button> conversion of the remaining span controls» to stage B; the combat cards were not covered by UA-02 either. Verified by code reading + simulated (DOM attributes). | When a card is clickable set `tabindex="0" role="button" aria-pressed=isSel` (and `aria-label` = name + status) and handle Enter/Space via the same onclick; move the selection outline to `box-shadow` or a class so it does not collide with the `:focus-visible` outline. |
| **CA-03** | U1 | **P1** | `_bcDialogClosed` (logic.js 148–153); `endCombat` «Продолжить» handler 2722–2724; luck choice handler 3068; `goTo` 2317–2321 | Focus return requires the opener to survive: `if(prev&&document.body.contains(prev)&&prev.offsetParent!==null){ … prev.focus(…) }`. The combat opener is the «⚔ Вступить в бой» button (renderChoices 2238–2242, activeElement at open time). On victory the button does `document.getElementById('modal-combat').classList.remove('on');renderGame();` — `renderGame` synchronously rebuilds `#c-list` (`list.innerHTML=''`, 2231) BEFORE the MutationObserver microtask delivers `_bcDialogClosed`, so `document.body.contains(prev)` is false and no focus is restored; the focused `#btn-combat-round` is inside a now `display:none` overlay, so the browser's focus fixup drops focus to `<body>`. Same for the luck dialog (choice → `goTo` → `renderGame`) and the offer modal when it was opened by a re-rendered choice. After every one of the 76 combats a keyboard user restarts Tab order from the first sidebar control (`#flask-use` on desktop) instead of the post-combat choices. Verified by code reading (event-ordering argument); focus itself not exercised (no layout in jsdom). | In `_bcDialogClosed`, when the recorded opener is gone (or absent), fall back to focusing the first `#c-list button`, else `#s-area` with `tabindex="-1"`. |
| CA-04 | U3 | P2 | `showItemNotification` (logic.js 408–415) | `el.setAttribute('role','status');el.setAttribute('aria-live','polite');` then `el.innerHTML=…` and only then `document.body.appendChild(el)` — the live region enters the DOM already populated. Screen readers announce changes to an EXISTING live region; a pre-filled region inserted afresh is announced inconsistently (browser/AT dependent), so stat notifications («− 3 ВЫНОСЛИВОСТИ», gold, «🍴 …») may be silent. The UI-04 resolution says «item/gold notifications are role=status aria-live=polite» — the attributes are present, the timing defeats them. Suspected (AT behaviour), code reading verified. | Keep one persistent `#bc-notif-live` `.sr-only` `role="status"` in the shell and write the joined message into it in a `setTimeout(…,0)` (the same pattern UA-03 uses), leaving the visual toast as is. |
| CA-05 | U3 / U2 | P2 | `renderDiceRoll` 2021–2036, `renderDiceCheck` 2063–2073, `renderDiceLoot` 2126–2133; §781, §932, §793/§887/§910/§1187 | On click the roll button does `list.innerHTML='';` and appends a plain `<div>` result («🎲 5», «🎲 3 + 4 = 7») + «Продолжить»: the result has no live region (unlike `#luck-result`) and the activated button is destroyed, so focus drops to body. Verified by code reading. | Give the result `<div>` `role="status"` (or write it to the persistent live region of CA-04) and `cont.focus()` after appending «Продолжить». |
| CA-06 | U3 | P2 | BC_COMBAT_STATUS mirror (logic.js 224–231); `startCombat` 2376–2391 | Reset heuristic `if(n<seen) seen=0;` fails when a new fight's synchronous intro batch has at least as many lines as the previous fight's final count (e.g. previous fight resolved at open by «Смерть Орков» = 2 lines; next fight with modifier + script line + orc-kill line = 3): the intro lines are never announced. **Simulated on dist:** after «a · b», a `innerHTML=''` + three appends in one task announced only «z». UA-03 resolution: «resets when the log is cleared» — only when the count shrinks. Verified by code reading + simulated (synthetic log). | Expose a reset hook called from `startCombat` right after `innerHTML=''` (e.g. `window._bcCombatStatusReset=()=>{seen=0;}`), or in the observer treat a record whose `removedNodes.length>addedNodes.length-1`… simpler: reset on any record with `removedNodes.length>0 && log.children.length<=addedNodes.length`. |
| CA-07 | U2 | P2 | Menu «📂 Импорт сохранения» (shell 819–822) | `<label class="btn btn-s menu-btn" style="display:block;cursor:pointer"><span …>📂 Импорт сохранения</span><input type="file" accept=".json" style="display:none" onchange="importSave(event)"></label>` — the input is `display:none` (not focusable) and the label has no `tabindex`, so import is unreachable by keyboard; the menu Tab trap cycles select → export → new game → close. Verified by code reading. | Replace `style="display:none"` on the input with `class="sr-only"` (focusable, still invisible) or give the label `tabindex="0"` + Enter/Space → `input.click()`. |
| CA-08 | U2 / U8 | P2 | `renderInvModalCurrent` 468; `.event-log-close` shell 751; `#inv-add-btn` shell 651; sidebar eat/drop 923–924; HUD chips shell 594–597; greyed spell choices 1620–1624 / 1646–1650; visual-dock pills 3246–3257 | Accessible-name/state quality: offer-modal drop `<button … title="${t('vybrosit')}">🗑</button>` → name from content = «🗑» (title is only a description); event-log close `<button class="event-log-close" onclick="toggleEventLog()">✕</button>` → name «✕»; `#inv-add-btn` → name «+» (`title` «Добавить предмет вручную» not the name); sidebar `aria-label="${t('vybrosit')}"` / «Съесть (+3 вын.)» identical for every row (no item name); HUD chips `<span class="hud-chip" title="Мастерство">…<b id="hb-skill">` — the label is `title` only, the icon is `aria-hidden`, so a screen reader hears «10/10 20/20 10 15» unlabeled; unavailable spell choices get `opacity:.35;cursor:not-allowed` + `title` but no `aria-disabled`; `.visual-pill` toggles carry no `aria-pressed`. Verified by code reading. | `aria-label` from the existing keys (`ui_title_add_item`, `ui_btn_close`, `vybrosit` + item name), `.sr-only` label spans in the chips reusing `ui_lbl_*`, `aria-disabled="true"` on greyed choices, `aria-pressed` on the pills. |
| CA-09 | U2 | P2 | `renderSpellSel` 311–331; `updateHUD` 920–924; `renderInvModalCurrent` 459–470 | Each of these rebuilds its container with `innerHTML` on activation: `grid.innerHTML=''` on every +/- click (ten presses are needed to start), `il.innerHTML=''` on every eat/drop (UA-02 made the buttons focusable but the rebuild destroys the focused button), `cur.innerHTML=…` on every modal drop. Focus falls to body after each action; inside a dialog the Tab trap recovers at the first control, on the spell and game screens the user restarts from the top of the document. Verified by code reading. | After each rebuild refocus the equivalent control by key (`[data-id][data-d]`, row index) or the section heading with `tabindex="-1"`. |
| CA-10 | U4 | P2 | `.event-log-panel` shell 365–368, `.event-log-btn` 360–363 + mobile.css 82–85, `.modal` mobile.css 131–137, `.visual-dock` shell 252 | (a) `.event-log-panel{position:fixed;right:-420px;top:0;bottom:0;…}` has no safe-area padding; on phones it becomes `width:100%` (mobile.css 86–89) so its sticky header with the «✕» close sits at `top:0` — under the status bar / Dynamic Island whenever the top inset is non-zero (standalone/fullscreen), and the panel is a full-screen layer without dialog semantics (no Escape, no focus move; HUD remains visually covered). (b) The FAB inset rule `.event-log-btn{bottom:calc(12px + var(--safe-bottom))}` lives only inside `@media (max-width:700px)`, where `showScr` hides the FAB (`isMobileHud()`); at 701–900 px landscape the FAB is shown at `bottom:20px;right:20px` with no inset. mobile.css comment 32: «The bottom inset stays with the components that already add it (choices-area, event-log FAB)» — for the FAB that is dead code. (c) `.modal{max-height:100dvh;…}` inside an overlay whose content box is `100dvh − safe-top`, with `align-items:center`, overflows equally top/bottom under the notch. (d) `.visual-dock{left:12px}` has no left inset. All suspected in effect (insets are 0 in a browser tab; real in standalone/fullscreen); code reading verified. | `.event-log-panel{padding-top:var(--safe-top);padding-bottom:var(--safe-bottom)}` (+ right inset), move the FAB inset rule out of the ≤700 block, `.modal{max-height:calc(100dvh - var(--safe-top,0px) - var(--safe-bottom,0px))}`, `left:calc(12px + var(--safe-left,0px))`; treat the phone-width panel as a dialog (add `.modal-overlay`-like semantics) or scope `width:100%` with a close-on-Escape. |
| CA-11 | U4 | P2 | mobile.css header 5–6 vs the landscape rule 160 | «Targets: Android Pixel 7a / iPhone 15 (412 CSS px portrait, 915 landscape)» but the sidebar-landscape rule is `@media (min-width: 701px) and (max-width: 900px) and (orientation: landscape)` and the desktop CSS starts above 900 px — a 915 px-wide landscape phone (the file's own primary target) gets the full desktop layout (320 px sidebar, 25 px text, `.modal{max-height:85vh}`, no FAB inset). iPhone 15 landscape (852 px) is covered. Verified by code reading. | Raise the upper bound of the landscape rule (and the `≤900px` touch block) to cover ≥ 915 px, e.g. `max-width: 960px`, or key the phone-landscape rule on `(max-height: 500px)`. |
| CA-12 | U6 | P2 | `.hud-bar` shell 446–456 | Height budget by code reading: padding 5+6, border 1, row 1 = chip height (13 px text × inherited `line-height:1.7` ≈ 22 px + 1+1 padding + 1+1 border ≈ 26 px), gap 4, row 2 = 36 px → **≈ 78 px**, against the resolution's «compact HUD bar (~70px …)» and the audit's 56–72 px target. On a 360 px phone row 1 fits (four chips ≈ 42 px each + name with ellipsis). Verified by code reading (depends on the inherited line-height). | `.hud-chip{line-height:1.2}` and/or `.hud-bar{padding:4px 10px}` brings it to ≈ 68 px without changing the buttons. |
| CA-13 | U6 | P2 | `returnSheetSection` logic.js 201–204, `SHEET_SECTIONS` 190 | `var ref=(h.next&&h.next.parentNode===h.parent)?h.next:null; h.parent.insertBefore(h.node,ref);` — if a moved node's recorded `next` sibling is another node moved in the same call, `ref` resolves to `null` and the node is appended at the END of the sidebar (after the menu button). Latent only: the current pairs (`inv:['sb-flask','sb-inv']`) are not adjacent (`.sb-gold`, `#sb-spells` sit between them), and the simulated round-trips restored the exact order. Verified by code reading (latent). | Return the nodes in reverse order of removal, or record `next` as the first sibling that is not itself in the moved set. |
| CA-14 | U7 | P2 | `src\fonts\OFL.txt`; UI-07 resolution («The license text now ships as src/fonts/OFL.txt (covers Cinzel, Cormorant Garamond and Forum)») | The file carries a single copyright header: `Copyright (c) 2011, Denis Masharov <denis.masharov@gmail.com>,` / `with Reserved Font Name "Forum".` followed by the OFL text. No copyright notice / Reserved Font Name for Cinzel (Natanael Gama) or Cormorant Garamond (Christian Thalmann); OFL §1 requires the copyright notice(s) of each font to accompany redistributed copies. `fonts.css` 8 says «license text in ./OFL.txt» for all three families. Verified by code reading. | Prepend the two missing copyright/RFN notices (they ship in the Google Fonts zips as OFL.txt headers) to `OFL.txt`. |
| CA-15 | U7 | P2 | `fonts.css` 79–86; `.modal h2` shell 271, `.stat-info h3` 112, `.event-log-header h3` 371 | Forum is declared once with `font-weight: 400` and the woff2 is `Forum / Regular` (usWeightClass 400); `h2`/`h3` keep the UA default `font-weight:bold`, so Cyrillic headings («⚔ Бой!», «Мастерство», «📜 Журнал событий») render Forum with synthetic bold while Latin glyphs get real Cinzel 600–700 — mixed weight rendering between RU/UK and EN/FR. Cosmetic; not determinable visually from the files (C14 is still pending the author's eyes). | `.modal h2,.stat-info h3,.event-log-header h3{font-weight:400}` (Cinzel has 400 in range) or `font-synthesis:none` on `--font-ui` users. |
| CA-16 | U8 | P2 | `locale.fr.js` `ui_autosave_note`; `locale.uk.js` `ui_lbl_map` / `ui_title_map_open` vs `ui_aria_map*` | FR value is `Sauvegarde auto :` with U+0020 before the colon (codepoints `… 0061 0075 0074 006F 0020 003A`), against the group_77 convention (NBSP U+00A0 before «:») that the brief extends to new ui keys. UK mixes terms: visible «🗺 Мапа» / «Відкрити мапу (клавіша M)» vs aria «Карта» / «Шар карти» / «Наблизити карту». Verified by code reading. | `Sauvegarde auto\u00a0:`; pick «мапа» or «карта» consistently for the UK map keys. |
| CA-17 | U2 / U9 | P2 | `#sb-map` shell 663 (`<div class="map-section" id="sb-map" style="display:none">`); `map_module.js` 406–413; `ui_title_map_open` «Открыть карту (клавиша M)» | `#sb-map` has been `display:none` since the shell's first commit (`6079283`) and no code in `game_logic.js`/`map_module.js` shows it, so the UI-04 «mini-map card … keyboard-operable» control and the sidebar «Открыть» button are never displayed; on desktop the only entry to the map is the hotkey, which tests `ev.key.toLowerCase()==='m'` — layout-dependent (a Russian layout yields «ь»), while `ev.code==='KeyM'` would not be. The hotkey is also not gated on the game screen or on open dialogs (it opens the map over the combat/luck/offer dialogs; coherent thanks to UA-01, but unexpected). Pre-existing, not a stage A/B regression. Verified by code reading. | Use `ev.code==='KeyM'` (plus `ev.key` fallback), gate on `#scr-game.on`, and either show `#sb-map` on desktop or add a map button to the sidebar footer. |
| CA-18 | U2 | P2 | `#riddle-input` shell 421–425 vs the ring rule 471 | `#riddle-input{…outline:none;}` (1,0,0) beats `input:focus-visible` (0,1,1), so the gold ring never applies; the field relies on `#riddle-input:focus{border-color:var(--gold);background:rgba(200,150,42,.06);}` — visible but inconsistent with every other control. Moot until CA-01 is fixed. Verified by code reading. | Drop `outline:none` from `#riddle-input` (the ring rule then applies). |

---

## 3. COUNTS

**P0: 1 · P1: 2 · P2: 15** (18 rows).
**Single highest-confidence finding: CA-01** — `renderRiddle` writes to a container (`#choices`) that has never existed in the shell; confirmed both by code/git reading and by running the built HTML in jsdom, where all seven riddle paragraphs kept the previous paragraph's choice buttons («По правой дороге (1219)») and never produced `#riddle-input`.

---

## 4. NOT-CHECKED

- **Live rendering / visual acceptance** (C14–C16 are still «pending the author's eyes»): font rendering, faux-bold appearance (CA-15), the exact HUD height (CA-12 is arithmetic on the CSS), safe-area behaviour on real notched devices (CA-10 applies only where `env(safe-area-inset-*)` is non-zero — standalone/fullscreen; in a normal browser tab the insets are 0). Not determinable from provided files.
- **Assistive-technology behaviour**: whether a given screen reader announces the pre-populated toast (CA-04) or the mirror text; only the DOM/timing facts are asserted.
- **Focus behaviour** (initial focus, focus return, Tab trap, `:focus-visible`): jsdom has no layout, so `offsetParent` is always `null` and `_bcFocusables` returns nothing; every focus statement in this report is by code reading. The Escape path could not be exercised in the sandbox because jsdom's nwsapi does not match `[onclick*="closeModal("]` (valid CSS in browsers) — verified only by code reading.
- **Why `#sb-map` is hidden** (CA-17): the `display:none` is present from the first shell commit; the author's intent is not determinable from provided files.
- **Track B / mechanics** were not examined (out of Track A scope), and no web sources were used.
- **Windows-only reproduction of the battery**: run once as instructed; not re-run on POSIX.
