# REGISTRY EXCERPT for the 2026-09 recheck cycle (generated from assets/text_corrections.json v2.155)

Generated 2026-09-04 by the maintainer session. This is a faithful extract of the adjudication ledger: the group_79 / group_80 resolutions below are the SPECIFICATIONS the code must be verified against (canon FB2 <-> resolution <-> code/data). Disagreement with a resolution itself must be filed as a finding with new evidence and the exact item id.

## 1. version_history since the last cycle (v2.139 -> v2.155)

- **v2.139 -> v2.140** - July 2026 - group_79 stage A batch 1: UI-02 component CSS. The unstyled combat contract (twelve selectors incl. the invisible HP bar and four status-pill states) and the visual dock are now styled on the existing token palette; body.hide-inline-art actually hides .illustration-container so the Illustrations toggle works. Three selector-presence asserts in _dist_ui_check (16 -> 19). CSS-only; dist rebuilt; battery ALL GREEN.
- **v2.140 -> v2.141** - July 2026 - group_79 stage A batch 2: UI-08 spell picker feedback. Latent t-shadowing crash fixed (renderSpellSel local renamed to tot); the live 'X iz 10 vybrano' counter chip finally exists (aria-live, gold, next to the now-decorative aria-hidden pips - the iz/vybrano keys had been waiting in all four locales); +/- get localized aria-labels, real disabled at both bounds and 44x44 targets. Two ui keys x4 (316), _dist_ui_check 19 -> 21. Dist rebuilt; battery ALL GREEN.
- **v2.141 -> v2.142** - July 2026 - group_79 stage A batch 3: UI-05 contrast + touch tokens. --muted and --red2 raised to the audit's values (#8a7a9e / #d6574b) for consistent small-text contrast; new --tap:44px token replaces the mobile touch block's 32px minimums (which sat under a 44x44 WCAG comment) and the list widens to include .inv-eat and map-control buttons; .qty-btn base rule moves to the token. The >=16px interactive-text sweep folds into UI-12's tokenization debt by design. Two asserts in _dist_ui_check (21 -> 23). CSS-only; dist rebuilt; battery ALL GREEN.
- **v2.142 -> v2.143** - July 2026 - recheck cycle 2026-07-20 report received and archived (SHA-256 55a0bd35...). group_80 opened with 12 OPEN findings (7 P0 + 3 P1 + 2 P2): the food-object pipeline gap the G-12 risk note predicted, mandatory-grant silent skips, staged Weakness targeting, dice persistence, joins x non-round kills, the condition-button leak into the next combat, the reopened wound_2 dead-end for instant kills, the sec.436 reload window, the FB2 weightless-weapons rule, the lost choice index double-charge, the four missing food slug map entries and the GAME_RULES version label. Adjudication to follow in P0 batches.
- **v2.143 -> v2.144** - July 2026 - group_80 batch 1 (P0): V-01 + R-02, the food pipeline. auto_items food entries normalized to the engine's one true shape before dedupe/modal; the offer modal gains a slot-free cap-guarded 'Eat now' action honouring FB2's 'mozhete syest srazu' (eaten entries lock both buttons; the update loop now targets the take button by class); four food slugs added to both runtime maps with a p1_6d registry==runtime parity guard. One ui key x4 (317), p1_6d 33 -> 34. Dist rebuilt; battery ALL GREEN.
- **v2.144 -> v2.145** - July 2026 - group_80 batch 2 (P0+P1): V-02 + G2-01. Imperative canon grants get a mandatory auto_items.equip branch that bypasses the closable offer modal: sec.71 atomically swaps the carried Green Knight sword for Death of Orcs, sec.1213 guarantees the shield; three ui keys x4 (320). The FB2 intro weightless-weapons rule lands as an armament class - whole_sword/death_of_orcs/knight_shield are kind:'weapon' slotCost:0 in the registry and zero-sized in ITEM_SIZES with a p1_6d two-way parity guard (34 -> 35). Dist rebuilt; battery ALL GREEN.
- **v2.145 -> v2.146** - July 2026 - group_80 batch 3 (P0): the combat lifecycle cluster X-01+X-02+X-03. One victory primitive (combatResolved over ALL non-fled enemies) now rules every kill path; staged joins fire after Copy/ally side-fights; active-wipe with round-only waiters keeps the fight open with an explicit bide-their-time beat (one ui key x4, 321). The condition button becomes a single idempotent recomputed component cleared by clearCombatExtraButtons at every boundary (start/round/end/routed) with instance-bound handlers - the stale-handler leak into the next fight is dead, and the sword-activated sec.617 goblin shows the flee exit immediately. endCombat records ALL non-flee condition exits on full victory (enemy_dead => wound_2 satisfied), closing the reopened sec.532/sec.994 dead-end for instant kills. p1_6d 35 -> 36. Dist rebuilt; battery ALL GREEN.
- **v2.146 -> v2.147** - July 2026 - group_80 batch 4 (P0): V-03 staged Weakness targeting. cs.weakPickIdx decouples the pre-round-1 Weakness pick from the round target: all alive cards (staged waiters included) are clickable with a red outline while the pick is open, round-1 consumption prefers the pick with the old default as fallback, and the per-enemy debuff rides a waiter into activation - FB2's 'any of them' honoured on sec.1175 and sec.628 bridges. p1_6d 36 -> 37. Dist rebuilt; battery ALL GREEN.
- **v2.147 -> v2.148** - July 2026 - group_80 batch 5 (P0, the last P0): V-04 dice persistence. Both fate rolls commit persisted records BEFORE showing their outcome: sec.781 restores the same faces and single resolved target on F5/revisit (no reroll-to-success), sec.932 restores the same n and unfinished pickup, and Continue without picking explicitly commits the refusal into diceLootDone. MANUAL_SMOKE_CHECKS C10/C11 added per convention. p1_6d 37 -> 38. Dist rebuilt; battery ALL GREEN.
- **v2.148 -> v2.149** - September 2026 - group_80 closing batch (P1+P1+P2): X-04 atomic sec.436 Force spend (flag consumed in the fight onclick, not at render - F5 no longer burns the paid cast), R-01 choice-index plumbing through the four post-luck renderChoices branches (withIdx map; shopBought/batchPicked keyed by real indices; normalizeSave prunes legacy [null] / ':undefined' markers) and R-03 GAME_RULES.md label sync with a standing 6d guard (label == last version_history transition). MANUAL_SMOKE_CHECKS C12/C13 added. group_80 12/12 DONE - verification cycle closed. p1_6d 38 -> 42. Dist rebuilt; battery ALL GREEN.
- **v2.149 -> v2.150** - September 2026 - group_79 stage A, batch 1 (P2+P2): UI-09 map localization + aria (Reset/Fullscreen/fog placeholder -> ui keys; +/- and the layer select get accessible names via the new data-i18n-aria channel in applyStaticI18n; 2C-SHELL harness extended; map refresh reordered after the static pass) and UI-10 motion/feedback (global prefers-reduced-motion rule, :active pressed states, autosave indicator in the menu). 7 ui keys x4 locales (321 -> 328). GAME_RULES.md label 2.149 -> 2.150 (R-03 guard). Dist rebuilt; battery ALL GREEN.
- **v2.150 -> v2.151** - September 2026 - group_79 stage A, batch 2 (P1+P1): UI-04 a11y foundation (shared dialog controller: role/aria-modal/labelledby, initial focus, focus return, guarded Escape, Tab trap; :focus-visible ring; live regions on notifications and the luck result; label-for, aria-label and keyboard access for the span/div controls) and UI-06 viewport/safe-area (viewport-fit=cover, insets on the fixed shell layers, dvh fallbacks, clamp padding on preface/pregame, short-viewport title-screen rule). _dist_ui_check 23 -> 33 (printed total). GAME_RULES.md label 2.150 -> 2.151 (R-03 guard). Dist rebuilt; battery ALL GREEN. Stage A complete - stage B (UI-03/UI-07/UI-11/UI-12) awaits the user's A/B/C decisions.
- **v2.151 -> v2.152** - September 2026 - group_79 stage B, batch 1 (P1): UI-07 Cyrillic display font per decision B - Forum (OFL) bundled for the Cyrillic range, font chains Cinzel / Cinzel Decorative -> Forum -> Cormorant Garamond -> serif, OFL.txt added, the unused Veles Redone and Cyrillic Old Face removed from fonts.css / src/fonts / README (dead base64 payload gone from dist), _dist_ui_check asserts for the new face and for the absence of the removed ones, MANUAL_SMOKE_CHECKS C14. Stage-B decisions recorded as author_decision: UI-03 A, UI-11 A, UI-12 B (deferred to the Midjourney art backlog). GAME_RULES.md label 2.151 -> 2.152 (R-03 guard). Dist rebuilt; battery ALL GREEN.
- **v2.152 -> v2.153** - September 2026 - group_79 stage B, batch 2 (P2, decision A): UI-11 reading rhythm - 72ch reading column (.reader) centred in the edge-to-edge scroll container, choices placed right after the text under a 'Your choice' heading in the same scroll flow (UI-01's separate capped block retired), mobile.css caps removed, ui_your_choice x4 locales, dist asserts swapped, MANUAL_SMOKE_CHECKS C15. GAME_RULES.md label 2.152 -> 2.153 (R-03 guard). Dist rebuilt; battery ALL GREEN.
- **v2.153 -> v2.154** - September 2026 - group_79 stage B, batch 3 (P1, decision A): UI-03 mobile information architecture - the sidebar is replaced on phones by a compact HUD bar (stats/gold chips + spells/bag/notes/map/log/menu buttons), sidebar sections are served as bottom sheets that reparent the live DOM nodes (BC_MOBILE_SHEETS), the log FAB is docked into the HUD, the landscape sidebar rule is scoped to 701-900px. 2 ui keys x4 (329 -> 331), dist asserts +4 (dialog roles 8), MANUAL_SMOKE_CHECKS C16. group_79 11/12 - only UI-12 (deferred to the Midjourney art backlog) remains open. GAME_RULES.md label 2.153 -> 2.154 (R-03 guard). Dist rebuilt; battery ALL GREEN.
- **v2.154 -> v2.155** - September 2026 - registry-only: memory-backlog reconciliation. The 'engine backlog' carried by session memory and HANDOFF_2026-07-20 s.4 (spell decrement P0, paragraph arithmetic + bird guide, Vodyanoy, infinite arrows, sec.688, gold signs, passive effects, luck dead-ends, Green Sword, Gold Key, offer/text gates, Magic Belt, stat list) verified as already resolved in the registry and GD - graveyard entry memory_backlog_reconciliation_2026_09_04 added to resolved_no_op so no future session re-queues it. Open work is now: group_71 art (Midjourney), UI-12 (deferred), group_68 sec.63 (deliberately deferred). GAME_RULES.md label 2.154 -> 2.155 (R-03 guard). No code change; battery ALL GREEN.

## 2. group_80_2026_07_20_recheck (12 items)

> Verification-cycle audit companion (brief audit_cycles/recheck_2026_07_20/BRIEF.md). Report received via chat 2026-07-20, SHA-256 55a0bd358a78779f1ca6053300fb2a9509b848430829a81e5e04040f4f8fc262, archived verbatim as audit_cycles/recheck_2026_07_20/REPORT.md (the user places the exact received file; the SHA is verified by apply_group80.js before commit). Report audited main @ 2179619 / v2.139; HEAD at group creation is b64167b (v2.142) - the three intervening commits are group_79 UI CSS batches that do not touch the mechanics under adjudication. Auditor scope: 16/16 group_78 resolutions verified (8 clean), 60 deterministic interaction scenarios, 13/13 mandatory pair families, G2 reverse search 109/109 stat candidates explained, full A-F regression green. Findings: 7 P0 + 3 P1 + 2 P2 - dominated by interaction/lifecycle defects the shape harnesses do not execute. Each item is a hypothesis pending FB2/resolution adjudication.

### V-01_food_object_pipeline - P0 - DONE
- **hypothesis (auditor):** sec.389 auto_items food-object {food,stamina}: the offer modal renders NAYDENO:[object Object], the taken entry stays a raw {food:...} object (HUD [object Object], no eat button) - the engine only recognizes {kind:'food',id,...}. Also FB2 grants 'mozhete syest srazu': with a full bag the modal only offers a disabled Take, so immediate eating is impossible even after fixing the shape. Fix: normalize {food,stamina} -> {kind:'food',id,stamina} before dedupe/modal; add a slot-free 'Eat now' action for food offers respecting the stamina cap.
- **resolution (spec):** CONFIRMED - the exact risk recorded at G-12 implementation time materialized. Fixed at the pipeline mouth: ai.items entries are normalized ({food,stamina} -> {kind:'food',id,stamina}) BEFORE dedupe and the modal, so canonItem/invDisplay/takeItem and the bag's eat button all see the engine's one true food shape - no [object Object] anywhere. The FB2 right 'mozhete syest srazu' is now a real slot-free action: food rows in the offer modal gain an 'Eat now' button calling the new eatFoundItem - stamina-cap-guarded exactly like eatFood (full-stamina shows the existing notice), marks the entry _eaten (take turns into a disabled fork icon, eat disables itself, no eat-then-take duplicate), logs, notifies and re-renders. The button-update loop now targets .inv-take-btn specifically so the eat button never inherits take-state styling (previously querySelector('button') grabbed the first button). Per-visit like the rest of the offer modal - not persisted across reload by design, matching takeItem. One ui key x4 (316 -> 317).

### V-02_mandatory_grant_swap - P0 - DONE
- **hypothesis (auditor):** sec.71 / sec.1213: both imperative grants ride the CLOSABLE voluntary showInventoryModal - closing without Take (or a full bag disabling the button) leaves inventory=[] and routes to 755, silently skipping Death-of-Orcs / knight_shield. With whole_sword already carried, sec.71 does not remove the old sword: both swords and both benefits coexist, against FB2 'Ostavte svoy mech v kladovoy vmesto togo, kotoryy berete'. Fix: an atomic mandatory acquisition/swap operation - sec.71 removes the current sword and grants death_of_orcs with no silent skip; sec.1213 guarantees the shield.
- **resolution (spec):** CONFIRMED against FB2's imperatives and fixed together with G2-01 (whose weightless-weapons rule removes every full-bag edge here). New auto_items.equip branch runs BEFORE the voluntary offer modal and cannot be skipped: on first entry sec.71 atomically removes the carried whole_sword if present ('Ostavte svoy mech... vmesto togo' - the hero's abstract starting sword needs no inventory op) and takes death_of_orcs in hand; sec.1213 guarantees the shield the same way. Notifications and event-log entries ('left behind for the new one' / 'taken in hand', three ui keys x4) ride the existing auto-notification flow; dedupe-safe on revisit (first-visit auto semantics unchanged, hash entry included). The sec.35 Green-Knight sword pickup stays a voluntary acquires choice by canon ('mozhete vzyat') - and, being weightless now, can never be blocked by a full bag either.

### V-03_weak_pick_staged_targets - P0 - DONE
- **hypothesis (auditor):** ENEMY_WEAK_PICK x staged bridges (116->1175, 655->628): before round 1 only the leader is active, clicks on the two waiting cards are ignored and the targetIdx validator resets to 0 - the -2 weakDebuff ALWAYS lands on the leader, contradicting FB2 'lyubogo iz Orkov'. The non-staged control 470->536 works. Fix: during pendingWeakPick before the first strike, make ALL alive cards selectable (decouple pick-targetability from active-targetability), record the chosen index, then restore the normal rule.
- **resolution (spec):** CONFIRMED against FB2 'lyubogo iz Orkov / lyubogo iz nih' and fixed by decoupling the two selections. New cs.weakPickIdx lives independently of the round-target index: while cs.pendingWeakPick is open (pre-round-1), EVERY alive non-fled card - staged waiters included - is clickable and takes a distinct red outline; clicking also updates the normal round target only when the card is legitimately active, so getCombatTarget's validator never fights the pick. Round-1 consumption prefers the picked enemy (alive-guarded) and falls back to the current round target when the player never clicked - preserving the old default; the debuff lands as the per-enemy weakDebuff, so a waiter carries its -2 into activation exactly as canon intends, and the outline clears once consumed. The non-staged control path (470->536) is behaviourally unchanged, as is target switching after round 1 (the auditor's own sec.536 verification). No data or locale changes; p1_6d pins the pickable predicate, the weakPickIdx plumbing and the guarded consumption (36 -> 37).

### V-04_dice_persistence - P0 - DONE
- **hypothesis (auditor):** sec.781 / sec.932: the roll result is not committed before showing the outcome. 932: after rolling, Continue without picking leaves diceLootDone unset - hash/revisit offers the roll again; F5 after a roll rerolls. 781: after a shown failure the save carries no result - reload re-offers the mandatory check until success. sec.725 by contrast persists every stamina loss. Fix: persist a resolved record for each at roll time; 781 restores the single resolved target on reload; 932 restores the same n and unfinished pickup, and Continue explicitly commits done.
- **resolution (spec):** CONFIRMED and fixed with the auditor's exact recipe - persisted resolved records committed BEFORE the outcome is shown. sec.781: the roll writes S.diceCheckDone[section]={a,b,ok,tgt} and saves before rendering the result, so F5 after a shown failure restores the SAME dice faces and the single resolved target, and any revisit lands straight on the resolved screen - the mandatory check can no longer be rerolled to success. sec.932: the roll writes S.diceLootRoll[section]={n} and saves before display; reload/revisit restores the same n and the unfinished pickup UI instead of a fresh roll, and Continue WITHOUT picking now explicitly commits the refusal into the existing S.diceLootDone (revisits show only Continue - the exact semantics the G-11 resolution promised). The pickup path itself is unchanged (slots, overflow note, one-larva consumption). sec.725 needed nothing (the auditor verified its per-roll persistence). Old saves migrate trivially: absent records simply mean unrolled. p1_6d pins both record plumbings and the refusal commit (37 -> 38); MANUAL_SMOKE_CHECKS gains C10/C11 per the new convention.

### X-01_joins_nonround_kills_victory - P0 - DONE
- **hypothesis (auditor):** Copy/ally kill paths and the victory checks use getAliveCombatEnemies (waiting excluded) and skip activateStagedJoins: sec.177 Copy kills the lion -> lioness stays hp=8 active=false but combatDone=true with Continue; useAllyInCombat has the same gap; sec.388 with both active knights dead and the third waiting, combatRound() ends in victory before round 11. Fix: a single combatResolved(cs) spanning ALL non-fled enemies used by every kill/victory path; call activateStagedJoins after Copy/ally kills; with round-only waiting and no active enemies keep the fight open with an explicit Wait action.
- **resolution (spec):** CONFIRMED - both summon side-fights and both combatRound alive-checks judged victory by getAliveCombatEnemies (waiting excluded) and the summons never triggered staged joins. Fixed with one primitive: new combatResolved(cs) = every enemy dead-or-fled, now the ONLY victory test on every kill path (combatRound x2, larva, Copy, ally, Death-of-Orcs - the Copy and ally tails turned out to be byte-identical, so a single exp:2 patch covered both). activateStagedJoins now runs after Copy/ally resolutions too, so the sec.177 lioness wakes when the Copy fells the lion. When actives are wiped but round-only joiners still wait (sec.388 third knight before round 11), the fight stays OPEN: a 'remaining foes bide their time' log line (one ui key x4), the round button advances the round as an explicit wait, and the sec1175-script path keeps its own management.

### X-02_condition_btn_leak - P0 - DONE
- **hypothesis (auditor):** #combat-condition-btn is not removed by clearCombatExtraButtons: after full victory it remains next to Continue, and after starting the NEXT combat (e.g. sec.43) it persists with the stale sec.617->165 handler - clicking can charge 2 STAMINA, mark the new fight resolved and route to the wrong paragraph. Also with death_of_orcs the goblin activates before round 1 but the flee button only appears after another full round. Fix: manage condition buttons in one container cleared on start/end/routed-end/navigation, recompute after ANY hp/active change (sword, larva, Copy, ally, join), bind handlers to an immutable section/combat instance.
- **resolution (spec):** CONFIRMED - the button was appended to #combat-buttons while clearCombatExtraButtons only removed the #combat-buttons-extra wrapper. Fixed structurally: the builder is extracted into a single idempotent updateCombatConditionButtons(cs) (remove-then-recompute; the per-condition log line now dedupes via cs.condMsgShown instead of relying on the button's existence), the clear helper also removes #combat-condition-btn - and since startCombat, every combatRound, endCombat AND endCombatRouted already call it, the leak dies at every boundary at once. Handlers are bound to the captured section+combat instance and no-op if either changed. Recompute now also runs after every non-round hp/active change (larva, Copy, ally, Death-of-Orcs at fight open - which also fixes the sub-finding: the sec.617 goblin activated by the sword shows the flee exit immediately, not a round late).

### X-03_wound2_instant_kills - P0 - DONE
- **hypothesis (auditor):** Instant kills (larva at sec.532/sec.994; Copy/ally paths) produce wounds=0 and call endCombat(true) without setting the Met flag - after Continue visibleChoices=[] and the exact dead-end G-04 fixed is reopened for non-trade kills. Fix: on full victory treat all non-flee combat_condition exits as available (enemy_dead => wound_2 satisfied for navigation), applied before post-combat render regardless of kill method.
- **resolution (spec):** CONFIRMED - the auditor's exact semantics adopted: full victory implies every damage-milestone, so endCombat(won) now records S.combatCondMet for ALL non-flee combat_condition exits unconditionally (enemy_dead => wound_2/enemy_defeated_1 satisfied for navigation), regardless of kill method - trade blow, larva, Copy, ally or the orc-slaying sword. The flee-only enemy_active_1 stays excluded exactly per the G-03 rule (no phantom escape after winning). Mid-fight button availability still uses the true wound counters via combatCondMet; only the post-victory record is unconditional. The sec.532/sec.994 dead-end is closed for every kill path.

### X-04_sec436_reload_window - P1 - DONE
- **hypothesis (auditor):** The sec.436 return branch performs S.sec436_force=false; saveGame() at RENDER time, before the Fight click. The net modifier on click is correct (+1 script +2 equipment), but F5 between render and click loses the already-paid Force and reverts to the pre-luck flow. Fix: consume the persisted flag in the atomic fight-start onclick (or store a committed combat setup restorable after reload).
- **resolution (spec):** CONFIRMED - render-time consumption opened a paid-cast loss window. The sec.436 Force-return branch no longer clears the flag while drawing the button: S.sec436_force=false + saveGame() moved into the Fight-at-+1 onclick, immediately before startCombat, so the spend commits atomically with the fight it pays for. F5 between render and click now reloads back onto the same +1 branch with the flag intact; the pre-luck flow stays unreachable until the fight actually starts. sec.526 round-trip otherwise unchanged (set at cast, cleared at fight start). MANUAL_SMOKE_CHECKS C12 added per convention.

### G2-01_weightless_weapons - P1 - DONE
- **hypothesis (auditor):** NEW FB2 evidence (intro section): 'dengi i oruzhie v zaplechnyy meshok ne kladutsya' - yet whole_sword and death_of_orcs sit in S.inventory and getBagUsed() counts each as a slot (a two-sword trace gives bagUsed=2). Not documented as a simplification in G-09/GAME_RULES; can block canonical pickups. Fix: registry slotCost:0 / kind:'weapon', size taken from the registry instead of the incomplete ITEM_SIZES; migrate both swords; keep the V-02 atomic swap separate.
- **resolution (spec):** CONFIRMED - the FB2 intro rule 'dengi i oruzhie v zaplechnyy meshok ne kladutsya' is direct new evidence. Implemented as an armament class: whole_sword, death_of_orcs and knight_shield become kind:'weapon' with slotCost:0 in the registry, mirrored in the runtime ITEM_SIZES (the pragmatic runtime source, since items.json is not compiled into dist); getBagUsed/getItemSize flow through unchanged, so both swords and the shield now occupy zero of the seven slots and canonical pickups can no longer be starved by armament. The shield is included by adjudication: it is carried on the arm as part of the knight's armament, not bagged - the auditor scoped the two swords, the extension is recorded here. Gold was already slot-free. A p1_6d guard asserts registry kind:'weapon' <-> ITEM_SIZES zero parity in both directions (three today; the next weapon cannot ship half-registered). The V-02 atomic swap is kept separate per the auditor's note.

### R-01_choice_index_lost - P1 - DONE
- **hypothesis (auditor):** Final renderChoices branches call makeChoiceBtn(ch) WITHOUT the source index: buying one-shot wood_piece at sec.340 stores shopBought:{'340':[null]} - after reload the button is active again and charges gold twice; pickup_batch stores the key '585:undefined' (works while one batch per paragraph, two would collide). Fix: pass the original sec.choices index through every render branch; add save round-trip tests for purchases and batches; migrate legacy [null]/:undefined saves.
- **resolution (spec):** CONFIRMED - the four post-luck renderChoices branches (lucky / unlucky / nonLuck x2) filtered sec.choices into fresh arrays and called makeChoiceBtn(ch) with no source index: makePurchaseBtn keyed shopBought with undefined (serializes as [null]; includes(undefined) is false after reload, so the sec.340 one-shot re-armed and charged gold twice) and makeBatchBtn keyed batchPicked as '585:undefined'. Fix: withIdx=sec.choices.map((ch,idx)=>({ch,idx})) feeds all three luck filters and every branch calls makeChoiceBtn(e.ch,false,e.idx), matching the other five call sites - original indices survive filtering. Legacy-save migration in normalizeSave: shopBought values must be arrays (others dropped) and are pruned to finite numbers ([null] gone); batchPicked backfilled and keys not matching ^\d+:\d+$ deleted. Lenient by design: an unknowable marker unlocks the offer again (symmetric pay-and-receive) instead of locking it out forever. p1_6d gains a source-shape guard plus a normalizeSave round-trip check; MANUAL_SMOKE_CHECKS C13 added.

### R-02_food_slug_maps - P2 - DONE
- **hypothesis (auditor):** The four new food slugs (melon/coconut/bun/provisions) are registered in items.json (105/20) but MISSING from both runtime maps SLUG_TO_RU/RU_TO_SLUG (101 entries): after a batch the HUD shows 'coconut (eda: +3)' etc. instead of RU names; melon joins the same path once V-01 lands. Fix: add the four entries to both maps (or generate the display map from items.json) plus a registry==runtime keys guard.
- **resolution (spec):** CONFIRMED - G-12 registered the four slugs in items.json but never extended the runtime maps (the G-09 batch extended them for its own two items, which is why this gap is G-12-specific). Four entries added to both SLUG_TO_RU and RU_TO_SLUG, and the auditor's guard request is implemented as a p1_6d assert: EVERY items.json key must appear in the SLUG_TO_RU literal and EVERY legacyRu in the RU_TO_SLUG literal (registry==runtime, both directions) - the next new item cannot ship half-mapped.

### R-03_game_rules_version_label - P2 - DONE
- **hypothesis (auditor):** assets/GAME_RULES.md status line still says v2.138 while version_history ends v2.138 -> v2.139 (the file was written before the F-01 bump). Mechanic numbers are current; metadata defect. Fix: update the status to the current registry version (and ideally derive/check it from the last version_history transition).
- **resolution (spec):** CONFIRMED - metadata drift only. Status label bumped v2.138 -> v2.149 in the same batch that creates the v2.149 transition, and p1_6d gains a standing guard: the **vX.Y** label in assets/GAME_RULES.md must equal the target of the last version_history transition, so any future registry bump without a label touch fails the battery. The frozen audit-cycle copy under audit_cycles/full_audit_2026_07_14/ is untouched by design.

## 3. group_79_2026_07_14_ui_ux_audit (12 items)

> UI/UX audit companion for cycle 2026-07-14 (received via chat 2026-07-14, SHA-256 1cb78719a211395167bd6366d7e885d7f8a76d471f29cffca95106781e740175, archived verbatim in audit_cycles/full_audit_2026_07_14/UI_AUDIT.md). Static DOM/CSS analysis (no live render was possible for the auditor). Overall verdict: the visual system needs no restyle; the risk is interface architecture. 1 P0 + 6 P1 + 5 P2. These are DESIGN decisions pending user review; the report proposes a three-stage rollout (blockers -> mobile IA -> visual polish) and a six-viewport acceptance matrix.

### UI-01_choices_scroll_architecture - P0 - DONE
- **hypothesis (auditor):** .main is overflow:hidden and .choices-area has no scroll/max-height; sec.132 renders 25 choice buttons (sec.340: 15; 42 paragraphs have >=5), so bottom purchases/exits can be unreachable on desktop and mobile. Preferred fix: one vertical scroll-flow for text+art+choices with fixed HUD; quick fix: dvh cap + overflow-y:auto; shops may deserve a grouped scrollable catalog with an always-visible exit.
- **resolution (spec):** Quick safe fix applied exactly per the audit's own fallback recipe: the .choices-area rule gains overflow-y:auto with a dvh cap and vh fallback (desktop 52dvh; phone portrait 34dvh; phone landscape 44dvh; tablet portrait inherits the desktop cap) plus overscroll-behavior:contain so inner scrolling cannot chain into the story. Paragraphs with few choices are visually unchanged (natural height stays below the cap); sec.132's 25 buttons and sec.340's 15 now scroll inside the capped area with the first option immediately visible and the last reachable by scroll - the audit's UI-01 acceptance criterion, satisfied statically. The story-area keeps its own scroll untouched. A regression assert lands in _dist_ui_check (15 -> 16) pinning the built artifact's cap. The report's PREFERRED variant (single vertical scroll-flow for text+art+choices with a fixed HUD, and a grouped scrollable shop catalog) remains an optional stage-1 redesign pending the user's design decision - this fix removes the P0 unreachability without prejudging that architecture.

### UI-02_unstyled_components - P1 - DONE
- **hypothesis (auditor):** JS builds .visual-dock/.visual-pill and toggles body.hide-inline-art, and combat DOM uses .combat-enemy-head/.ce-status-pill/.state-*/.combat-stat-pill/.combat-hp-track/.combat-hp-fill - none of these selectors have CSS in src or dist, so the art toggle cannot hide illustrations and the combat HP bar/status pills render unstyled. Either finish the component CSS to the DOM contract or remove the dead elements; add a selector-presence test.
- **resolution (spec):** Chose the audit's option A - finish the component CSS to the DOM contract; the dock and toggles turned out to be LIVE JS (ensureVisualDock is called from initVisualPolishV1), so only the stylesheet layer was missing. Twelve combat selectors now styled on the existing token palette: head layout with the skull icon, status pills with all four state variants exactly matching updateCombatEnemyDisplay's class swap (active green / dead red / waiting gold / fled muted-italic), stat pills, and the previously INVISIBLE HP bar - an 8px track with a red-gradient fill driven by the existing inline width%. The visual dock renders as fixed bottom-left pills with an .on gold glow, respecting --safe-bottom (fallback 0), and body.hide-inline-art now actually hides .illustration-container (both mj-art and legacy-scan variants) - the Illustrations toggle works for the first time. G-05's card selection outline/cursor stays inline by design (state-driven per frame). Three selector-presence asserts added to _dist_ui_check per the audit's own request (16 -> 19). Pure CSS + tests + registry: no DOM/JS changes, goldens untouched. Live visual acceptance by the user pending (screenshots).

### UI-03_mobile_information_architecture - P1 - DONE
- **author_decision:** A (2026-09-04): full mobile redesign - compact sticky HUD (stats, gold, menu, log) with spells/bag/notes/map as bottom sheets; desktop layout untouched. Implemented together with UI-11 A as the game-screen rebuild series.
- **hypothesis (auditor):** At <=700px the sidebar becomes a 38vh scroll container competing with the 62vh story scroll while choices sit in a non-scrolling bottom and the floating log FAB can cover the last button. Proposal: compact 56-72px sticky HUD (stats+gold+menu) with spells/bag/notes/map in bottom sheets and the log docked in the HUD.
- **resolution (spec):** DECISION A DONE (phones <=700px, both orientations; desktop and the 701-900px landscape layout untouched): the sidebar is hidden and a compact HUD bar (~70px: name + skill/stamina/luck/gold chips, then six icon buttons - spells, bag, notes, map, log, menu - as 36px tap targets) sits under the safe-area inset; spells, bag(+flask) and notes open as bottom sheets (max 82dvh, rounded top, close button) by MOVING the live sidebar nodes into the sheet body and returning them home on close, so updateHUD, inventory, spells and notes keep writing to the same ids - no duplicated rendering; the HUD chips mirror the stat cells through a syncHudBar hook wrapped around updateHUD; the map button opens the existing full map overlay, the log button docks toggleEventLog (the floating FAB is hidden on phones via showScr plus a matchMedia listener), the menu button opens the menu. The sheet is a .modal-overlay member, so the UI-04 dialog controller gives it role/aria-modal, focus, Escape and the Tab trap for free; leaving the phone breakpoint with a sheet open closes it and restores the nodes. The 701-900px landscape sidebar rule is now scoped with min-width:701px so it never fights the HUD layout. Two ui keys x4 (ui_hud_toolbar, ui_btn_close; 329 -> 331), four dist asserts, dialog-role assert 7 -> 8, MANUAL_SMOKE_CHECKS C16.

### UI-04_keyboard_screenreader - P1 - DONE
- **hypothesis (auditor):** 37 shell buttons carry no role/aria-*; overlays lack dialog semantics, focus trap, Escape and focus return; .btn{outline:none} with no :focus-visible; mini-map is a clickable div and several controls are spans; no live-regions for notifications/combat log; the name label is not bound via for. Proposal: shared dialog controller + real buttons + gold focus ring + aria-live/log + axe-core pass.
- **resolution (spec):** STAGE A (foundation) DONE: (1) a shared dialog controller (BC_A11Y_DIALOGS) observes the 'on' class on every .modal-overlay/.end-overlay - static or runtime-created - and gives it role=dialog + aria-modal, an accessible name from its first heading, initial focus on the first focusable control, focus return to the opener on close, Escape (only when the dialog offers an explicit closeModal control, so combat/luck/death/item-offer dialogs cannot be dismissed) and a Tab trap; the existing open/close call sites are untouched. (2) The seven static overlays carry role=dialog / aria-modal / aria-labelledby in markup as well (their headings got ids). (3) A :focus-visible gold ring for button/.btn/.choice-btn/select/input/textarea/[tabindex] restores the keyboard focus visibility that the outline:none resets removed, without touching pointer interaction. (4) Live regions: item/gold notifications are role=status aria-live=polite; the luck-roll result is aria-live=polite. The combat log and the event log rebuild via innerHTML on every update, so a live region there would re-announce the whole history - converting them to appendChild is a stage-B refactor before they can become role=log. (5) The hero-name label is bound with for=, the add-item input gets an aria-label (ui_ph_add_item), and the mini-map card plus the add-item toggle become keyboard-operable (role=button, tabindex=0, Enter/Space). Real <button> conversion of the remaining span controls and an axe-core pass stay with the stage-B UI-03/UI-12 work. Ten _dist_ui_check asserts added (printed total 23 -> 33, retro-covering UI-09/UI-10 as well).

### UI-05_contrast_touch_targets - P1 - DONE
- **hypothesis (auditor):** --muted (3.85:1) and --red2 (3.48:1) are used on 10-14px text (42 small-size declarations); several touch controls are 32x32 or smaller despite the 44x44 comment. Proposal: raise muted to ~#8a7a9e and red to ~#d6574b, introduce a --tap:44px token, keep interactive text >=16px.
- **resolution (spec):** Implemented with the audit's own values. Token bumps: --muted #7a6a8e -> #8a7a9e and --red2 #c0392b -> #d6574b - global, so every muted/danger use (combat log wounds, deadline lines, status pills, the new HP-fill gradient endpoint) lightens consistently on the dark palette; --red stays deep for backgrounds/glows. New --tap:44px token; the smoking gun the audit spotted is fixed - mobile.css's touch block carried a '44x44 WCAG target' comment while setting 32px minimums - now min-width/height:var(--tap,44px), and the list widens from three selectors to five (.inv-eat and .map-controls button join .inv-remove/.event-log-close/.qty-btn); the shell .qty-btn base rule moves from 30x30 to the token (UI-08's inline 44s become redundant-consistent). Deliberate scope note: the exhaustive interactive-text >=16px sweep across the 42 small-size declarations is the same debt as UI-12's gradual inline-style tokenization and folds into it rather than being half-done here. Two token asserts in _dist_ui_check (21 -> 23). Live visual acceptance pending.

### UI-06_responsive_viewport_safearea - P1 - DONE
- **hypothesis (auditor):** Fixed title screen can overflow on landscape phones (no height/orientation breakpoints); preface/pregame keep inline 40/60px padding on mobile; 100vh instead of dvh; safe-area padding sits on body while screens are position:fixed; no viewport-fit=cover. Proposal: dvh + clamp padding + safe-area on the fixed shell + landscape testing.
- **resolution (spec):** STAGE A DONE: the viewport meta gains viewport-fit=cover; safe-area insets move from body (which never reached the position:fixed screens) to the fixed shell layers - .scr and .modal-overlay get the top/right/left insets, .end-overlay keeps its 40px base on top of them, and the bottom inset stays with choices-area and the event-log FAB (no double buffer); the item-notification toast offsets by the top/right insets; the mobile .modal cap and the map shell height gain dvh with vh fallback; the preface/pregame inline 40/60px padding becomes clamp(20px,5vw,40px) clamp(16px,7vw,60px); and a max-height:600px rule lets the title screen scroll on landscape phones (auto-margin centring instead of justify-content clipping, 52dvh rider art, tighter ornament/description). Height/orientation breakpoints for the game screen itself remain part of the stage-B mobile-IA decision (UI-03).

### UI-07_cyrillic_display_font - P1 - DONE
- **author_decision:** B (2026-09-04): dedicated Cyrillic display face (Forum); chain keeps Cormorant Garamond as the safety net; remove the unused fonts from the project.
- **hypothesis (auditor):** Cinzel/Cinzel Decorative ship Latin-only unicode-range with bare serif fallback, so RU/UK headings render in system serif while EN/FR get Cinzel - identity varies by language/OS. Minimum: add Cormorant Garamond (already bundled with Cyrillic) as the explicit ui/title fallback; ideally a licensed Cyrillic display face metrically compatible with Cinzel.
- **resolution (spec):** DECISION B DONE: Forum (Denis Masharov, SIL OFL 1.1, Google Fonts v19 cyrillic subset, 16.5 KB woff2) is bundled as src/fonts/Forum-cyr.woff2 and declared for the Cyrillic range only (U+0301, U+0400-045F, U+0490-0491, U+04B0-04B1, U+2116), so both display chains become Cinzel / Cinzel Decorative -> Forum -> Cormorant Garamond -> serif: Latin glyphs keep Cinzel, Cyrillic glyphs get Forum's inscriptional capitals instead of the OS serif, and Cormorant (option A) remains the per-glyph safety net. The license text now ships as src/fonts/OFL.txt (covers Cinzel, Cormorant Garamond and Forum). Per the user's instruction the two never-referenced faces with unusable licensing (Veles Redone: template copyright; Cyrillic Old Face: no declared license) were removed from fonts.css, src/fonts/ and the README, and _dist_ui_check now asserts their absence from the built artifact (they had been embedded as dead base64 payload). Two dist asserts added for the Forum face and the font chain; MANUAL_SMOKE_CHECKS C14 covers the visual check on RU/UK vs EN/FR.

### UI-08_spell_picker_feedback - P2 - DONE
- **hypothesis (auditor):** Spell selection shows ten pips but no X/10 text counter (JS looks for a missing #spell-counter-chip); +/- lack accessible names, no real disabled at bounds. Proposal: live 'Selected X of 10', aria-labels, disabled, 44px targets; keep pips decorative.
- **resolution (spec):** CONFIRMED, plus a latent crash discovered en route: renderSpellSel declared const t=totSp(), SHADOWING the translation function, and the counter line then called t('iz') on a number - the missing #spell-counter-chip was the only thing keeping the code from throwing (the 'iz'/'vybrano' ui keys already existed in all four locales, so the feature was fully intended and half-wired). Fixed by renaming the local to tot everywhere in the function (pips, maxed class, start-button gate) and adding the chip element next to the pips: a gold aria-live=polite line reading 'X iz 10 vybrano' in the locale's own words, updated on every render. The pips bar is now aria-hidden (decorative, as the audit asked). The +/- buttons gain localized aria-labels ('Add/Remove spell: NAME', FR with the NBSP colon; two ui keys x4, 314 -> 316), REAL disabled at the bounds (minus at zero, plus when ten are picked - recomputed each render since the grid rebuilds; the old silent-return guards stay as a backstop) with a dimmed disabled style, and 44x44 touch targets replacing 36x36. Two presence asserts in _dist_ui_check (19 -> 21). Live visual acceptance pending.

### UI-09_map_localization_a11y - P2 - DONE
- **hypothesis (auditor):** Map toolbar hardcodes English Reset/Fullscreen (initial fog-of-war line too); +/- have no labels; mini-map is pointer-only; mobile side panel takes up to 30vh before a dense 1600x1000 SVG. Proposal: localize the toolbar, add aria/titles, keyboard mini-map, mobile-first location/percentage summary, persist zoom/layer.
- **resolution (spec):** STAGE A (localization + aria) DONE; the remaining proposals (keyboard mini-map, mobile-first location summary, persisted zoom/layer) are folded into the stage-B mobile-IA decision (UI-03) and the a11y foundation (UI-04). The map toolbar no longer hardcodes English: Reset/Fullscreen become data-i18n keys (ui_btn_map_reset / ui_btn_map_fullscreen), the initial fog-of-war placeholder becomes ui_map_topbar_hint, and the +/- buttons and the layer select get accessible names through a new data-i18n-aria channel (ui_aria_map_zoom_in / _zoom_out / _layer) plus matching titles - applyStaticI18n gains the aria-label pass and the 2C-SHELL harness gains the aria key set (coverage + round-trip). Seven ui keys added x4 locales (321 -> 328; the seventh is UI-10's autosave label). The map refresh now runs AFTER the static shell pass in repaintAfterLangSwitch, so the localized placeholder never overwrites the live layer line when the overlay is open during a language switch.

### UI-10_motion_feedback - P2 - DONE
- **hypothesis (auditor):** Six keyframes and 18 transitions with no prefers-reduced-motion; almost no :active states for touch; frequent autosave with no saved-state indicator; alert/confirm for import/new game break the theme. Proposal: reduced-motion mode, pressed states, 'Autosaved - secN - time' in the menu, themed dialogs with undo/double-confirm for destructive actions.
- **resolution (spec):** STAGE A DONE: (1) a global prefers-reduced-motion rule collapses the keyframe animations and transitions to .01ms (iteration-count 1, scroll-behavior auto), so motion-sensitive players get the end states instantly with no layout change; (2) :active pressed feedback (brightness) for .btn, .choice-btn and the map controls; (3) an autosave indicator in the menu overlay - saveGame stamps lastAutosaveAt and renderAutosaveNote writes 'Autosaved: sec.N - HH:MM' (ui_autosave_note x4 locales) into an aria-live=polite line under the menu title, refreshed on every menu open; it reads the persisted timestamp rather than the clock, so an unsaved session shows nothing. Themed replacements for alert/confirm (import/new game) with undo/double-confirm stay deferred to the stage-B theming decision (UI-12).

### UI-11_reading_rhythm - P2 - DONE
- **author_decision:** A (2026-09-04): cap the reading column at 68-74ch and place the choices right after the text under a 'Your choice' heading - the single scroll-flow variant from UI-01; implemented in the UI-03 series.
- **hypothesis (auditor):** Story container allows ~1100px line length on wide screens; choices are permanently separated from the text by a bordered fixed block, weakening the read-then-decide rhythm. Proposal: cap #s-text/#c-list at ~68-74ch centered inside a full-width scroll container; place choices right after the text under a 'Your choice' heading.
- **resolution (spec):** DECISION A DONE: the reading column is now a .reader wrapper inside .story-area (max-width:72ch of the 25px Cormorant body face - roughly 70 characters per line - centred with auto margins; the scroll container itself still spans .main so the scrollbar stays on the screen edge), and the choices moved INTO that flow: .choices-area sits right after #s-text under a 'Your choice' heading (ui_your_choice x4 locales), separated by a hairline instead of the old fixed bordered block. The UI-01 quick-fix scroll cap (52dvh / 34dvh / 44dvh + overscroll-behavior) is retired - one scroll for text, art and choices, the audit's preferred variant; sec.132 (25 choices) and sec.340 (15) stay reachable by that single scroll. mobile.css drops the caps and moves the bottom safe-area buffer onto .story-area. _dist_ui_check swaps the UI-01 cap assert for two flow asserts; MANUAL_SMOKE_CHECKS C15.

### UI-12_icon_and_theming_debt - P2 - OPEN
- **author_decision:** B (2026-09-04): wait for the Midjourney subscription - the launcher mark joins the group_71 art backlog; the inline-style-to-tokens extraction is picked up opportunistically by the UI-03/UI-11 game-screen rebuild.
- **hypothesis (auditor):** The launcher icon's fine detail muddies at 32-64px (keep the illustration for splash; make a simple mark for favicon/launcher); 58 inline style attributes + 18 cssText + ~140 direct style ops hinder unified responsive/a11y patches - gradually extract to classes and tokens (surface/text/accent/danger/tap/focus/motion).

## 4. resolved_no_op - graveyard entries relevant to this cycle

### 9_combat_balance_outliers
- **previously_claimed:** undefined
- **verdict:** design_note_not_bug
- **reason:** Gemini 3.1 Pro audit flagged §36 (Trader Skill 7 / Stamina 17) and §760 (Bats Skill 4 / Stamina 6–8) as combat outliers. On review these are intentional pacing choices: the trader is a deliberate HP-sponge / mid-game choke point; the bats are a stamina-drain encounter by design. Neither is a bug. Listing as a design note in case a future balance pass revisits individual encounters, but no engine or data work is required.

### 10_spell_economy_warnings
- **previously_claimed:** undefined
- **verdict:** design_note_not_bug
- **reason:** Gemini 3.1 Pro audit observed that ILLUSION and HEALING spells have only 1–2 uses each across 1221 paragraphs while FIRE and LEVITATION are mandatory. The audit suggested either rebalancing the canon or warning the player at character creation. Both options are out of scope: rebalancing the canon would mean changing the source book; the character-creation warning is a UX feature that belongs in a separate pass and is not registry-tracked. Diagnostic only — no fix planned. Players read the canon and choose accordingly.

### memory_backlog_reconciliation_2026_09_04
- **previously_claimed:** Session memory and HANDOFF_2026-07-20 section 4 carried an 'engine backlog': P0 spell decrement on click (sec.93/7/415), paragraph-arithmetic per-item remaps (sec.13/140/198/385/471/600/612/688/1071/1137) and the bird-guide subgraph (sec.106/151/178/277), the Vodyanoy paradox (sec.873/642), infinite arrows (sec.570), sec.688 text truncation, six gold-sign bugs, five passive effects, luck-roll dead-ends (sec.203/289/377/456), Green Sword gating, Gold Key timing (sec.440/1172), item-offer gates (sec.16/810/972), text-only gates (sec.412/464/725/1208), Magic Belt (sec.575) and a stat-effect verify list (sec.74/444/453/1093/1128/1147/1181).
- **verdict:** already_resolved
- **reason:** Reconciled 2026-09-04 against the registry and the live GD data: every item is either VERIFIED_RESOLVED in pending_corrections (gold_sign_six, passive_effects_five, vodyanoy_paradox, sec570_infinite_arrows, sec688_text_truncation, registry_spurious_13_140_532, sec166_green_sword_set, item_offer_gates_16_810_972, magic_belt_timing, one_sided_luck_adjudication) or implemented in shipped data/engine: the single-spell choice branch spends the charge on click (useSpell(spellId) before applyChoiceAcquires -> goTo); the June 2026 ARITHMETIC_MAP batches 1-6 are all wired as inventory_condition choices (caged_bird on sec.156/201/228/327, ruby_star on all eight sites, mirror_secret on sec.297/677, gold_amulet on sec.1164 and sec.625, book/treasure_lore/pass/candle/thread_ball/castle_password singles, letter riddles as engine riddle fields on sec.1113/67/1131/992/435, combat summons via groups 61/63); inventory_condition gates exist on sec.412/464/1208; gold_key is granted at sec.440/1172 and checked at sec.1085; all seven stat effects are present as auto_items. The list went stale in June (groups 54-67) and must not be re-queued.

## 5. Items with status VERIFIED_RESOLVED (do not re-flag)

`gold_sign_six`, `passive_effects_five`, `sec688_text_truncation`, `vodyanoy_paradox`, `sec570_infinite_arrows`, `magic_belt_timing`, `item_offer_gates_16_810_972`, `sec166_green_sword_set`, `registry_spurious_13_140_532`

